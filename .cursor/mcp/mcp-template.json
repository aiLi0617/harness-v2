{
  "_README": [
    "这是 MCP 配置模板文件。使用步骤：",
    "1. 复制本文件为 .cursor/mcp.json",
    "2. 将所有 <PLACEHOLDER> 替换为实际值",
    "3. 删除所有 _note / _README 字段（Cursor 会忽略，但保持整洁）",
    "4. 环境相关的 MCP 按需复制并改名（如 dev-mysql-mcp → sit-mysql-mcp）",
    "",
    "命名规范：",
    "  通用 MCP：{服务}-mcp（如 ones-mcp、feishu-mcp）",
    "  环境 MCP：{环境}-{服务}-mcp（如 dev-mysql-mcp、sit-mysql-mcp）"
  ],
  "mcpServers": {
    "ones-mcp": {
      "_note": "ONES 缺陷管理。StreamSSE 协议，首次使用需在 Cursor 中完成 OAuth 授权。无需手动填密钥。",
      "url": "https://sz.ones.cn/mcp",
      "headers": {}
    },
    "feishu-mcp": {
      "_note": "飞书 API（云文档/消息/日历等）。StreamSSE 协议，URL 从飞书开放平台 MCP 插件获取。",
      "_url说明": "登录 https://mcp.feishu.cn 创建或获取你的 MCP 连接 URL",
      "url": "<YOUR_FEISHU_MCP_URL>",
      "headers": {}
    },
    "chrome-devtools-mcp": {
      "_note": "浏览器调试（页面检查/网络请求/截图等）。无需密钥，开箱即用。",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest"
      ]
    },
    "codegraph-mcp": {
      "_note": "可选。本地代码知识图谱（符号搜索/调用链/影响分析）。100% 本地，无需密钥。先在业务项目根目录执行 codegraph init -i 建立 .codegraph/ 索引。",
      "_安装说明": "默认 npx 零安装；若已 npm i -g @colbymchenry/codegraph，可将 command 改为 codegraph、args 去掉 -y 与包名",
      "command": "npx",
      "args": [
        "-y",
        "@colbymchenry/codegraph",
        "serve",
        "--mcp",
        "--path",
        "${workspaceFolder}"
      ]
    },
    "loki-mcp": {
      "_note": "可选。Grafana Loki 日志查询（LogQL）。ones-loki-trace-investigator 依赖此 MCP。未配置时该代理会要求用户手动提供日志。",
      "_安装说明": "运行 .cursor/scripts/init-loki-mcp.ps1 或 .sh（构建 Docker 镜像或本地二进制并写入 MCP 配置）",
      "_LOKI_URL说明": "Loki API 地址，如 http://192.168.3.25:3100；Docker 模式下容器需能访问该地址",
      "_认证说明": "无需认证可只填 LOKI_URL；多租户/鉴权时按需设置 LOKI_ORG_ID、LOKI_TOKEN 或 LOKI_USERNAME/LOKI_PASSWORD",
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-e",
        "LOKI_URL",
        "-e",
        "LOKI_ORG_ID",
        "-e",
        "LOKI_USERNAME",
        "-e",
        "LOKI_PASSWORD",
        "-e",
        "LOKI_TOKEN",
        "loki-mcp-server:latest"
      ],
      "env": {
        "LOKI_URL": "<YOUR_LOKI_URL>"
      }
    },
    "dev-mysql-mcp": {
      "_note": "环境相关。开发环境数据库访问。按需复制为 sit-mysql-mcp、uat-mysql-mcp 等。",
      "_MYSQL_HOST说明": "数据库主机地址，如 192.168.3.8",
      "_MYSQL_DATABASE说明": "目标数据库名，如 broker4agent",
      "_权限说明": "ALLOW_DDL=true 允许建表改表；ALLOW_DROP/DELETE=false 禁止删表删数据",
      "command": "npx",
      "args": [
        "mysql-mcp-server"
      ],
      "env": {
        "MYSQL_HOST": "<YOUR_MYSQL_HOST>",
        "MYSQL_PORT": "3306",
        "MYSQL_USER": "<YOUR_MYSQL_USER>",
        "MYSQL_PASSWORD": "<YOUR_MYSQL_PASSWORD>",
        "MYSQL_DATABASE": "<YOUR_DATABASE_NAME>",
        "ALLOW_DDL": "true",
        "ALLOW_DROP": "false",
        "ALLOW_DELETE": "false"
      }
    },
    "dev-swagger-mcp": {
      "_note": "环境相关。Apifox API 文档查询。每个项目有独立的 project-id 和 token。",
      "_project-id说明": "在 Apifox 项目设置中获取，替换 command 中的 <YOUR_PROJECT_ID>",
      "_APIFOX_ACCESS_TOKEN说明": "在 Apifox 个人设置 → API 访问令牌 中生成",
      "command": "cmd /c npx -y apifox-mcp-server@latest --project-id=<YOUR_PROJECT_ID>",
      "args": [],
      "env": {
        "APIFOX_ACCESS_TOKEN": "<YOUR_APIFOX_TOKEN>"
      }
    },
    "dev-nacos-mcp": {
      "_note": "环境相关。Nacos 服务注册与配置中心查询。",
      "_NACOS_ADDR说明": "Nacos 地址，格式 host:port，如 192.168.3.25:8848",
      "command": "uvx",
      "args": [
        "nacos-mcp-router@latest"
      ],
      "env": {
        "NACOS_ADDR": "<YOUR_NACOS_ADDR>",
        "NACOS_USERNAME": "<YOUR_NACOS_USERNAME>",
        "NACOS_PASSWORD": "<YOUR_NACOS_PASSWORD>"
      }
    }
  }
}
