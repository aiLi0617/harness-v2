# refactor-plan 使用说明

> 将模糊的"这段代码需要重构"转化为有序、安全、可验证的重构步骤序列。Agent 执行细节以 [`SKILL.md`](./SKILL.md) 为准。

## 这个 Skill 是做什么的

只读分析目标代码，识别坏味道、为每种坏味道选择对应的重构手法、拆分为可独立验证可回滚的原子步骤，最终输出一份详细到 `implementer` 可直接逐步执行的重构计划。本技能**不修改任何代码**。

- 主要产出：**`{artifact_root}/plans/refactoring-plan.md`**
- 下游：`safe-refactoring`（按计划逐步执行）

## 适用场景

- 收到明确的重构需求
- 开发过程中识别出代码坏味道
- 代码审查发现结构性问题需要系统性改善

## 执行流程

```
1. 识别坏味道   → 过长方法/重复代码/上帝类/特性依恋/数据泥团/过长参数列表 等
2. 选择重构手法 → 提取方法/提取类/搬移方法/引入参数对象 等
3. 拆分小步骤   → 单一操作、可验证、可回滚、有序依赖
4. 输出重构计划 → 坏味道清单 + 步骤表 + 风险评估 + 不做的事
```

## 产出物

| 文件 | 说明 |
|------|------|
| `{artifact_root}/plans/refactoring-plan.md` | 概要、坏味道清单、重构步骤表、风险评估、明确排除项 |

## 关键约束

- 本技能为**只读分析**，不修改任何代码
- 计划必须详细到 `implementer` 无需额外判断即可执行
- 涉及公共接口变更的坏味道必须标注高风险并建议与用户确认

## 相关资源

| 资源 | 关系 |
|------|------|
| [`safe-refactoring`](../safe-refactoring/SKILL.md) | 下游：按本计划安全执行重构 |
| `refactoring.yaml` | 重构工作流中调度本技能 |
