# codegen-guard 使用说明

> 在生成新代码前做分层、命名、复用、一致性的合规预检，防止"能跑但不合规"的代码进入仓库。Agent 执行细节以 [`SKILL.md`](./SKILL.md) 为准。

## 这个 Skill 是做什么的

在写入任何新文件（新类、新接口、新方法）**之前**执行一套 6 项检查清单，确认代码符合项目分层架构与编码规范。检查不通过则禁止创建文件，必须先修正。只针对**新增代码**，不对已有代码做合规扫描（那是 `quality-gate-reviewer` 的职责）。

- 由 `implementer` 在生成新文件前强制调用
- 检查结果记录到 `{artifact_root}/workflow/harness-debug.md`（经 `harness-debug-logger`）

## 适用场景

- 创建新的源代码文件时
- 在现有文件中新增类或接口时
- 任何子代理创建 `.java`/`.kt`/`.xml`/`.yaml` 等源文件前

## 检查清单（6 项）

| # | 检查项 | 关注点 |
|---|--------|--------|
| 1 | 分层合规 | Controller/Service/Repository/Entity/DTO 各司其职，依赖方向不反向 |
| 2 | 包路径 | 包名与目录一致，遵循模块化划分 |
| 3 | 命名合规 | 类/接口/方法/常量/枚举/包命名符合已加载规则 |
| 4 | 复用 | 创建前搜索是否已有可复用的类/基类 |
| 5 | 必需元素 | 类级 Javadoc、最小可见性、分层注解、日志声明 |
| 6 | 制品一致性 | 与 `lld.md`/`api-contract.md`/`ddl.md` 对齐 |

## 输出

- 通过：`代码生成合规检查 — 通过`，允许创建
- 阻塞：`代码生成合规检查 — 阻塞`，列出问题，修正后重新检查

## 关键约束

- 检查在写入文件**之前**执行，不是事后审查
- 不通过禁止创建文件
- 复用检查使用项目内搜索，不依赖外部索引

## 相关资源

| 资源 | 关系 |
|------|------|
| `rules-loader.mdc` | 执行前按场景加载命名、分层、方法设计等规则 |
| [`done-verify`](../done-verify/SKILL.md) | 事后的完成前验证门禁 |
| `harness-debug-logger` | 记录每次检查结果 |
