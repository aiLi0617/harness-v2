# git-worktree

使用说明以 [`SKILL.md`](SKILL.md) 为准。该 Skill 负责隔离并行代码工作区、
记录工作区映射和安全回收；是否并行、分配哪些任务由 Workflow 或主会话决定。

实现计划使用 `{artifact_root}/plans/implementation-plan.md`，工作区日志使用
`{artifact_root}/workflow/harness-debug-worker-{N}.md`。
