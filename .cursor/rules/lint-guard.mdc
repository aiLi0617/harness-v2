---
description: Lint 检查门禁；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# Lint 检查门禁

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 规则

- **不允许引入新的 lint 错误** — 变更前后 lint 告警数量不能增加
- 变更完成后检查 lint 输出
- 如果有新增告警，必须修复后再提交

### 执行方式

使用 IDE 的 lint 检查功能（ReadLints）或项目配置的静态分析工具：

- Checkstyle / SpotBugs / PMD（视项目配置）
- IDE 内置检查

### 处理流程

1. 完成代码变更
2. 对变更文件执行 lint 检查
3. 若有新增告警：修复 → 重新检查
4. 确认无新增告警后继续

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
