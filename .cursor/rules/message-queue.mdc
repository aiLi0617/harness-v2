---
description: 消息队列使用规范；在处理匹配文件或相关任务时使用
globs: "**/*Mq*.java,**/*MQ*.java,**/*Message*.java,**/*Consumer*.java,**/*Producer*.java"
alwaysApply: false
---

# 消息队列规范

## 适用范围

- 当引用、读取或修改匹配 `**/*Mq*.java,**/*MQ*.java,**/*Message*.java,**/*Consumer*.java,**/*Producer*.java` 的文件，或任务语义明确涉及本规则领域时适用。

## 强制规则

- MQ 与三方回调接入须评估租户隔离（跨租户禁止、`ignore-urls` / `ignore-tables` 识别口径）

### 基本原则

- 消息体须可序列化、可版本演进；禁止传递不可序列化的大对象引用
- 生产与消费须**幂等**设计，消费失败须有重试与死信策略
- 禁止跨租户投递消息；忽略租户的发送方法须按项目命名约定标识（如 `IgnoreTenantId` 后缀）

### 命名与主题

- Topic/Queue/Tag 命名见名知义，含业务域与环境标识
- 禁止硬编码 Topic 名，须走配置或常量类

### 消费规范

- 消费逻辑须捕获业务异常与系统异常并区分处理
- 禁止在消费线程中执行长事务或阻塞 IO 无超时
- 消费成功后须确认 offset/ack；失败须可追踪（日志含 messageId、traceId）

### 规划（提前规划）

项目启动前维护 MQ 登记表：Topic、生产者、消费者、消息体说明、重试策略、所属模块。

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
