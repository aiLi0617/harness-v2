---
description: 人工检查点 — 需要暂停征询用户意见的场景；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 人工检查点

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 必须暂停的场景

遇到以下情况**必须停下来问用户**，不得自行决定：

1. **需求模糊** — 无法从上下文明确判断意图
2. **方案抉择** — 存在多种合理实现方案，各有利弊
3. **风险操作** — 删表、修改接口签名、移除公共方法、修改数据库约束
4. **超出范围** — 当前变更可能影响任务范围以外的功能
5. **假设不确定** — 需要对业务规则做出假设

### 暂停后的操作

1. 向用户清晰描述问题和可选方案
2. 等待用户决策
3. 将问答记录追加到 `{artifact_root}/workflow/decision-log.md`

### Decision Log 记录格式

```markdown
### [日期] 阶段名称

**问题**: 需要决策的问题描述
**选项**:
- A: 方案描述 — 优点/缺点
- B: 方案描述 — 优点/缺点
**决策**: 用户选择的方案
**影响**: 该决策对后续工作的影响
```

### 下游要求

下游子代理启动时**必须读取** `{artifact_root}/workflow/decision-log.md`，确保遵循已有决策。

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
