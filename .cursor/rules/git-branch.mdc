---
description: Git 分支策略；在相关任务需要该约束时按需使用
globs:
alwaysApply: false
---

# Git 分支策略

## 适用范围

- 当任务语义明确涉及本规则领域，或用户显式引用本规则时按需加载。

## 强制规则

### 分支命名

| 类型 | 前缀 | 示例 |
|------|------|------|
| 功能开发 | `feature/` | `feature/user-auth` |
| 缺陷修复 | `bugfix/` | `bugfix/login-timeout` |
| 重构 | `refactor/` | `refactor/order-service` |
| 热修复 | `hotfix/` | `hotfix/payment-null-check` |

### 分支流程

1. 从 `develop` 拉出功能分支
2. 在功能分支上开发并提交
3. 开发完成后合并回 `develop`
4. 禁止直接在 `main`/`master` 上提交

### 规则

- 分支名全小写，单词用连字符 `-` 分隔
- 分支生命周期尽量短，完成后及时删除
- 合并前确保与 `develop` 同步（rebase 或 merge）

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
