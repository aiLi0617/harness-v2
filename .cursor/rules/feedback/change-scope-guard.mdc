---
description: 变更范围门禁
globs: 
alwaysApply: true
---

# 变更范围门禁

## 规则

- **diff 中每行改动必须可追溯** — 追溯到需求、缺陷修复或重构计划
- **禁止夹带无关改动** — 不搭便车修改无关代码
- **重构类变更须有重构计划** — 重构改动必须可追溯到明确的重构目标

## 检查方式

提交前审查 `git diff`：

1. 逐文件检查改动内容
2. 确认每处改动属于当前任务范围
3. 发现无关改动 → 拆分到独立提交或撤回

## 常见违规

- 顺手调整了无关类的格式
- 修复了不在当前任务范围内的 bug
- 添加了"以后可能用到"的工具方法

## 正确做法

- 无关改动记录到 TODO，另起任务处理
- 格式化变更如果涉及大量文件，单独一个提交

## 补充规则

- 需求文档/PRD/用户明确排除的功能，严禁编写对应代码——即使"顺手就做了"也不允许；实现前必须对照功能清单逐项确认范围
