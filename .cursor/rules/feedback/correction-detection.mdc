---
description: 纠正检测与规则自动沉淀
globs: 
alwaysApply: true
---

# 纠正检测

## 监听信号

检测用户消息中的纠正信号关键词：

- "我说过"、"又犯了"、"之前提过"、"第N次了"
- "不是这样"、"我之前说过"、"你又..."、"重复了"

## 触发条件

当检测到**同类错误第 2 次及以上**时，触发规则沉淀流程。

## 规则沉淀流程

1. 提取纠正内容，概括为一条明确的规则
2. 根据分类映射表确定目标文件
3. 将规则追加到对应 `.mdc` 文件的末尾（在 `## 补充规则` 章节下）

## 分类映射表

> 分类与 `orchestration/coding-standards-loader.mdc` 场景表、`agents/shared/memory-consolidator.md` 分类表保持一致,三处任一处新增分类时必须同步更新。

| 纠正类别 | 目标文件 |
|----------|---------|
| 命名 | `memory/naming-conventions.mdc` |
| 异常处理 | `memory/exception-handling.mdc` |
| 日志 | `memory/logging.mdc` |
| 空值 | `memory/null-safety.mdc` |
| 方法设计 | `memory/method-design.mdc` |
| 注释 | `memory/comment-conventions.mdc` |
| 数据库 | `memory/database-conventions.mdc` |
| API | `memory/api-design.mdc` |
| 测试 | `memory/testing-conventions.mdc` |
| 集合处理 | `memory/collection-handling.mdc` |
| 并发处理 | `memory/concurrency.mdc` |
| 日期时间 | `memory/datetime.mdc` |
| POJO/OOP | `memory/pojo-conventions.mdc` |
| 依赖管理 | `memory/dependency-management.mdc` |
| 项目架构/分层 | `memory/project-architecture.mdc` |
| 微服务/模块规划 | `memory/microservice-conventions.mdc` |
| 应用安全 | `memory/security-conventions.mdc` |
| Redis 缓存 | `memory/redis-caching-conventions.mdc` |
| OBS/对象存储 | `memory/object-storage-obs-conventions.mdc` |
| 代码格式 | `memory/code-style-format.mdc` |
| 控制语句 | `memory/control-flow-conventions.mdc` |
| 消息队列 | `memory/message-queue-conventions.mdc` |
| ORM/MyBatis | `memory/orm-mybatis-conventions.mdc` |
| 多租户隔离 | `memory/tenant-isolation.mdc` |
| Schema/Entity 一致性 | `feedback/schema-guard.mdc` |
| Java 编辑自检 | `feedback/java-edit-self-check.mdc` |
| Git 分支 | `orchestration/git-branch.mdc` |
| Git 提交 | `orchestration/git-commit.mdc` |
| 变更实施流程 | `orchestration/change-implementation.mdc` |
| 任务拆解 | `orchestration/task-decomposition.mdc` |
| 阶段契约 | `orchestration/stage-contracts.mdc` |
| MCP 配置/引用 | `memory/mcp-conventions.mdc` |
| ER 图绘制 | `memory/er-diagram-spec.mdc` |
| 无法归类 | 新建 `memory/<topic>.mdc` |

## 规则写入格式

```markdown
## 补充规则

- 规则描述
```
