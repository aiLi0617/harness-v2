---
description: ER 图(Mermaid erDiagram)绘制风格规范
globs:
alwaysApply: false
---

# ER 图绘制规范

> 约束 ER 图(Mermaid `erDiagram`)绘制风格:字段编码、关系标注、分图、图例。
> 适用于任何需要绘制 ER 图的文档场景。

---

## 1. 实体声明

```
<节点ID> {
    <字段行>
}
```

- **节点 ID**:仅保留 `[a-zA-Z0-9_]`,其余字符替换为 `_`,连续 `_` 压缩为单个,首尾 `_` 删除。
- **缩进**:实体声明 4 空格,字段行 8 空格。
- **跨库 ER 图**:节点 ID 用 `{库}_{表}` 完整拼接,不缩写,如 `broker4listing_agreement`。

> ⚠️ **禁止使用别名语法 `<节点ID>["<别名>"] { ... }`**。Cursor 内置 mermaid 渲染器解析时会把它当作完整 token,导致后续关系行匹配不到实体,渲染出"空小框 + 完整字段框"双节点。详见 §9.1。

### 1.1 表说明段(mermaid 块外)

由于禁止别名,表的中文注释由 mermaid 块**之外**的"表说明" markdown 表统一维护:

```markdown
## 表说明

| 表 | 说明 |
|----|------|
| `agent_user` | 经纪人用户 |
| `asset_bank_card` | 经纪人银行卡 |
| `job_log` | — |
```

- 顺序与 mermaid 块中实体出现顺序一致。
- 无注释时填 `—`(em dash)。
- 注释内 `|` 替换为 `\|`、换行替换为空格。

---

## 2. 字段行编码(核心)

```
<字段名> <类型(大小)>_<Y|N>_<说明slug截断30> [PK|UK]
```

mermaid 渲染时各 token 的视觉列序:

| 列 | 内容 | mermaid 语法位置 |
|---|---|---|
| 1(最左) | 字段名 | `type` 位置 |
| 2 | `类型(大小)_可空_说明` 三段合一 | `name` 位置 |
| 3(最右) | `PK` / `UK` | `key` 位置 |

> ⚠️ **不使用 mermaid 原生 `"comment"` 列**:这是 9.2+ 才支持的功能,Cursor 内置渲染器不显示该列,会导致字段说明完全丢失。本规范把说明融合到第 2 列末尾,所有渲染器都能看到。详见 §9.4。
>
> 把"字段名"写在 mermaid 的 `type` 位置是利用 mermaid 不对 type/name 内容做语义校验、只看语法位置的特性,达到 DBeaver/Navicat 风格的视觉布局。

### 2.1 第 1 列:字段名

- 原样使用 DB 字段名,不做大小写转换。
- 特殊字符按节点 ID 规则清洗(仅保留 `[a-zA-Z0-9_]`)。

### 2.2 第 2 列:类型(大小)_可空_说明

格式:`{原始类型}[(大小)]_{Y|N}[_{说明slug截断30}]`

**类型(大小)**:保留 DB 原始类型,括号与逗号原样保留。
- `varchar(200)` / `decimal(30,8)` / `bit(1)` / `bigint`(无大小时不补) / `text` / `datetime`

**可空标记**:
- **`Y` = NOT NULL**(取自 "Yes, NOT NULL")
- **`N` = NULL(可空)**

**说明 slug 转义规则**:
1. 仅保留:中文(`\u4e00-\u9fff`)、英文字母、数字、下划线。
2. 其余字符(逗号、句号、括号、百分号、斜线、引号、空格、emoji、分号等) → 替换为 `_`。
3. 连续 `_` 压缩为单个;首尾 `_` 删除。
4. 超过 30 字符以 `...` 截断(含省略号共 ≤ 30 字符)。
5. 注释为空时省略整段(不输出多余的 `_`)。

**示例**:

- `bigint_Y_主键`
- `bigint_N_租户ID`
- `varchar(200)_N_云商订单编号`
- `decimal(30,8)_N_订单金额`
- `tinyint_N_订单状态_1_未生效_NOT_IN_EFFE...`
- `bit(1)_Y_是否删除_0否_1是`

### 2.3 第 3 列:键后缀

| 后缀 | 触发条件 |
|---|---|
| `PK` | 主键 |
| `UK` | 唯一索引 |

外键**不加 `FK` 后缀**(关联通过 §3 表达,避免冗余)。读者识别外键的两个信号:
1. 命名约定:以 `_id` 结尾(逻辑外键)。
2. 实体外的连线:`<父表> ||--o{ <子表> : <外键>_to_<父键>`。

### 2.4 字段顺序

- 按 DB 物理顺序(`ORDINAL_POSITION`)输出,不重排、不省略。
- 如需聚焦展示,使用占位 stub 实体(只保留 `id` 一列)。

### 2.5 完整字段行示例

```
id bigint_Y_主键 PK
tenant_id bigint_N_租户ID
agent_user_id bigint_N_经纪人ID
product_name varchar(255)_N_产品名称
order_amount decimal(30,8)_N_订单金额
order_status tinyint_N_订单状态_1_未生效_NOT_IN_EFFE...
deleted bit(1)_Y_是否删除_0否_1是
message_id varchar(64)_Y_消息唯一ID_UUID UK
```

---

## 3. 关系(双轨:mermaid 连线 + markdown 关系表)

> 同时维护两份是有意为之的"双保险":mermaid 连线提供视觉关联,markdown 表提供机读、兜底和精确语义。任一渲染器失败时,关系信息都不会丢失。

### 3.1 Mermaid 块内的关系连线

```mermaid
erDiagram
    <实体1> { ... }
    <实体2> { ... }
    <父表节点> ||--o{ <子表节点> : <外键>_to_<父键>
```

强制规则:

- **所有实体定义集中在前,所有关系连线集中放在 mermaid 块末尾**(不穿插)。
- 标签强制为 `{外键字段}_to_{父键字段}`(如 `agent_user_id_to_id`)。
- 节点名必须与 §1 实体声明完全一致(纯实体名,无别名)。
- 父表不在当前图但被外键引用时,作为 stub(仅 `id` 一列)拉入。

> 必须"实体在前、关系在后集中"的原因:部分渲染器解析关系行时会预创建被引用的空节点,若关系行先于实体定义出现,后续真实定义无法合并,产生同名双节点。详见 §9.2。

### 3.2 Mermaid 块外的同库关系表

```markdown
## 库内关系

| 子表 | 外键字段 | 基数 | 父表 | 父键字段 |
|------|----------|------|------|----------|
| `asset_bank_card` | `agent_user_id` | N:1 | `agent_user` | `id` |
```

- 列固定,便于机读、grep、Excel 导入。
- 与 §3.1 的连线一一对应,是兜底权威。

### 3.3 Mermaid 块外的跨库关系表

```markdown
| 子库.子表 | 外键字段 | 基数 | 父库.父表 | 父键字段 | 证据 |
|-----------|----------|------|-----------|----------|------|
| `broker4settlement.order` | `agreement_id` | N:1 | `broker4listing.agreement` | `id` | 业务主链 |
```

- 跨库表用 `{schema}.{table}` 完整限定。
- 多一列 `证据`(`业务主链` / `column_name` / `decision-log`)。
- 跨库关系**不画**在各库 mermaid 内,统一收口到跨库主链文档。

### 3.4 基数取值

| 关系表取值 | Mermaid 符号 | 语义 |
|---|---|---|
| `1:1` | `\|\|--\|\|` | 一对一强(少见) |
| `1:0..1` | `\|\|--o\|` | 一对零或一 |
| `1:N` | `\|\|--\|{` | 一对多(父必有子) |
| `1:0..N` | `\|\|--o{` | 一对零或多(**默认**,最常见) |
| `N:M` | `}\|--\|{` | 多对多(通常需中间表) |

未显式标注时按 `1:0..N` 解读。

### 3.5 逻辑外键判定(自动生成时)

字段被判定为外键须**同时满足**:

1. 字段名以 `_id` 结尾。
2. 不是 `id` 自身,也不是 `tenant_id`(见 §3.6)。
3. 去尾 `_id` 后能解析到表名(支持单复数:`user` ↔ `users`、`category` ↔ `categories`)。

解析优先级:同库 → 跨库时核心库优先。无法解析时不画线、不写入关系表。

### 3.6 `tenant_id` 不参与关系

- 不画连线、不写入关系表(避免每张图都连到租户表)。
- 但仍出现在字段列表中(保留 `tenant_id bigint_N_租户ID` 行)。

---

## 4. 业务表与框架/日志表分块

同库内同时存在业务表与框架/日志表时,**分两段 Mermaid 图**:

```
### 业务表
### 框架 / 日志表
```

典型框架/日志表命名模式:

| 模式 | 来源 |
|---|---|
| `^undo_log$` | Seata |
| `^_?job_log$` | XXL-Job |
| `^message_log$` | MQ 消息日志 |
| `^event_.*_log$` / `^event_consum_log$` / `^event_product_log$` | 事件总线 |
| `^dlq_message$` | 死信队列 |

业务表无关系且数量较少时,可不分块、合并为一张图。

---

## 5. 分图与字段预算

| 限制 | 数值 |
|---|---|
| 单张 Mermaid 图字段总数上限 | **400** |
| 超限处理 | 优先按业务模块拆分;无可拆模块时按字段贪心切块,标题加 `(i/N)` 后缀 |

拆分顺序:
1. 业务表 / 框架表二分(§4)。
2. 任一组仍超 400 字段时按字段贪心切块。
3. 表数极多的库使用业务聚类拆分(如订单与支付 / 银行与流水 / 配置/日志)。

---

## 6. 统一图例段

每张(或每组)ER 图前**必须**包含图例:

> **图例**:列序 `字段名 | 类型(大小)_可空_说明 | 键`。可空标记 **`Y` = NOT NULL**、**`N` = NULL**。说明融合在第 2 列末尾(下划线分隔,超 30 字符以 `...` 截断),因 Cursor 内置 mermaid 渲染器不显示原生 comment 列。键后缀 `PK` = 主键、`UK` = 唯一索引;外键不加后缀,关联见 mermaid 块之后的"库内关系"表格。表中文说明见 mermaid 块之前的"表说明"段。

---

## 7. 命名一致性

- ER 图中表名、字段名、注释**与数据库保持一致**,不修复 DB 中的错别字或不规范命名;命名修复走 DDL 变更流程后再重绘 ER 图。
- 表/字段注释为空或无意义时,在 ER 图中保留空白(slug 段缺省),不自行补全。

---

## 8. 反例

### 8.1 字段编码

| ❌ 错误 | ✅ 正确 | 原因 |
|---|---|---|
| `varchar_200_Y order_no_KPRI_订单号 PK` | `order_no varchar(200)_Y_订单号 PK` | 列序错;类型应保留括号 |
| `id bigint PK` | `id bigint_Y_主键 PK` | 缺可空标记/说明 |
| `name string_Y_姓名` | `name varchar(64)_Y_姓名` | 必须用 DB 原始类型 |
| `id bigint_Y PK "主键"` | `id bigint_Y_主键 PK` | 不用 mermaid 原生 `"comment"` |
| `..._订单状态_1_未生效_NOT_IN_EFFECT_0_履约中_performancing` | `..._订单状态_1_未生效_NOT_IN_EFFE...` | slug 超 30 字符须截断 |
| `decimal(30,8)_N_金额(单位:元)` | `decimal(30,8)_N_金额_单位_元` | slug 内括号/冒号必须替换为 `_` |
| `agent_user_id bigint_N FK_经纪人ID` | `agent_user_id bigint_N_经纪人ID` | 字段不加 `FK` 后缀 |
| Y/N 语义反了:NOT NULL 用 `_N` | NOT NULL → `_Y`、NULL → `_N` | Y 取自 "Yes, NOT NULL" |

### 8.2 关系

| ❌ 错误 | ✅ 正确 |
|---|---|
| `tenant_id` 写入关系表 | `tenant_id` 不参与关系(仅显示在字段行) |
| 跨库关系表用缩写 `agreement` | 完整限定 `broker4listing.agreement` |
| 同库关系表混入跨库关系 | 同库表只写同库;跨库单独成表(§3.3) |

### 8.3 其他

- ❌ ER 图旁同时维护重复的 Markdown 字段表(违反"单一权威")
- ❌ 用截图替代 Mermaid 文本
- ❌ 在 ER 图中"修正"DB 错别字(应走 DDL 变更)
- ❌ 单张图字段数超过 400 不拆分

---

## 9. 渲染兼容性(决策依据备忘)

本规范多项约束源于 Mermaid 渲染器实测踩坑,特别是 **Cursor 内置 mermaid 预览**对某些语法支持不一致。

| # | 决策 | 触发问题 | 对应规则 |
|---|---|---|---|
| 9.1 | 禁止 `entity["alias"]` 别名 | Cursor 把 `ENTITY["..."]` 当完整 token,后续关系行匹配不到实体,渲染出"完整框 + 空小框"双节点 | §1 禁用别名 + §1.1 用块外"表说明" |
| 9.2 | 关系行集中放在实体定义之后 | 部分渲染器解析 `A \|\|--o{ B` 时若 `B` 未定义,会预注册空节点,后续真实定义无法合并 | §3.1 先所有 entity,后所有 `\|\|--o{` |
| 9.3 | 字段名写在 mermaid 的 `type` 位置 | mermaid 官方列序为 `type name [key] "comment"`,但业务期望 DBeaver/Navicat 风格(字段名最左);利用 mermaid 不校验 type/name 内容只看位置的特性即可 | §2 列序映射 |
| 9.4 | 说明融合到第 2 列,不用原生 `"comment"` | 原生 attribute comment 是 mermaid 9.2+ 特性,**Cursor 内置渲染器不显示该列**,导致说明完全丢失 | §2.2 融合 + 30 字符截断 |
| 9.5 | mermaid 连线 + markdown 关系表"双保险" | 即便 9.1–9.3 全部遵守,仍可能有渲染器边界 bug;两份信息任一失效都不丢 | §3.1 + §3.2 |

> **未来升级**:若 Cursor 升级到 mermaid 9.2+ 并支持 attribute comment,可考虑切换回原生 `"comment"` 列。届时**必须先在 Cursor 实测渲染通过**,再修改 SPEC §2 与生成器。

**推荐验证渲染器**:[mermaid.live](https://mermaid.live)(最准)、GitHub Markdown、Cursor 内置预览(本规范兼容目标)、Typora。如 Cursor 内仍出现空小框,**先核对 §9.1 别名 与 §9.2 关系行位置是否被绕过**。
