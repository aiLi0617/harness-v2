---
description: 任务拆解规则；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 任务拆解规则

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 子任务粒度

- 每个子任务是"**一个可独立验证的变更**"
- 子任务的验证方式必须在拆解时明确
- 粒度参考：一个 Service 方法 + 对应测试 = 一个子任务

### 拆解要求

1. **标注依赖关系** — 明确哪些任务必须顺序执行
2. **识别可并行项** — 无依赖的任务标记为可并行
3. **每项有明确验证方式** — "怎么确认这个任务完成了"

### 子任务模板

```
### 子任务 N: <名称>
- 依赖: 子任务 X（或"无"）
- 变更范围: <涉及的文件/类>
- 验证方式: <如何确认完成>
- 预估复杂度: 低/中/高
```

### 原则

- 先完成基础设施（Entity、Mapper），再完成业务逻辑（Service），最后完成接入层（Controller）
- 拆解后先评审顺序合理性，再开始实施
- 实施过程中发现遗漏，随时补充子任务

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
