---
description: 应用安全规约
globs: "**/*Controller*.java,**/*Api*.java,**/*Service*.java"
alwaysApply: false
---

# 应用安全规约

## 权限与数据访问

- **水平权限校验** — 用户个人页面/功能必须做权限控制，禁止未校验即可访问、修改、删除他人数据
- **敏感数据展示脱敏** — 手机号等禁止明文展示（如 `137****0969`）
- **敏感权限入口** — 须严格校验身份与授权

## 输入校验

用户请求的任何参数必须做有效性验证，防止：

- page size 过大导致内存溢出
- 恶意 order by 导致慢查询
- 任意重定向
- SQL 注入（须参数绑定，禁止字符串拼接 SQL）
- 反序列化注入
- 正则 ReDoS（攻击者构造特殊输入导致死循环）

对外 RPC/API/HTTP 开放接口、执行耗时大、稳定性要求高、调用频次低的方法，**必须**做参数校验。

可省略校验的场景：

- 极可能被循环调用的方法（须在 Javadoc 注明外部校验要求）
- 底层高频 DAO 方法（与 Service 同机部署时）
- 确定调用方已校验的 private 方法

## Web 安全

- **禁止向 HTML 输出未过滤或未转义的用户数据** — 防 XSS
- **表单/AJAX 提交须 CSRF 验证** — 如签名验证
- **SQL 参数严格使用 `#{}` 绑定** — 禁止 `${}` 和字符串拼接 SQL

## 业务防滥用

使用短信、邮件、电话、下单、支付等平台资源时，须实现防重放：

- 数量限制、疲劳度控制、验证码校验
- 发帖、评论、即时消息等 UGC 场景须防刷、违禁词过滤

## 手册对齐（黄山版）

- **【强制】URL 外部重定向目标须白名单过滤** — 防钓鱼
- **【强制】文件上传**须严格校验大小与类型
- **【强制】配置文件中的密码须加密存储**
- **【强制】生产环境 API 须 HTTPS**

## 补充规则

- 新增对外接口时，同步评估：入参校验、权限注解、菜单权限 DML
- 日志中的敏感信息（手机号、身份证、银行卡等）须脱敏，禁止明文输出；展示层脱敏与本文件展示脱敏要求同时满足
