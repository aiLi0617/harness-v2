---
description: Java 枚举类型规范
globs: "**/*.java"
alwaysApply: false
---

# 枚举规范

## 命名

- 枚举类必须以 `Enum` 后缀（如 `ProcessStatusEnum`）
- 数据库字段对应的枚举存放在以 `{数据表名}Enum` 命名的枚举文件中（如 `shopping_cart` 表 → `ShoppingCartEnum`）

## 使用场景

- **固定范围变化用 enum** — 存在延伸属性时优先 enum 而非魔法字符串
- **枚举值必须全部被引用** — 禁止存在未使用的枚举常量；新增枚举值必须有对应的使用方，删除业务逻辑时须同步清理对应的枚举值

## 编码与传输

项目中所有枚举**一律使用 Integer 数字编码**（枚举类的 `code` 字段），**禁止在 VO/DTO/API 中使用字符串枚举名**。

- **前后端传输使用数字** — 请求和响应中的枚举字段类型为 `Integer`，值为枚举类的 `code`
- **VO/DTO 字段类型为 Integer** — 不使用 `String` 存储枚举值
- **@Schema 注释中标注含义** — `@Schema(description = "合同类型: 1=纸质合同, 2=电子合同, 3=不签合同")`
- **API 契约文档中标注映射关系** — 枚举定义表必须列出数字编码、含义、对应后端枚举类
- **禁止在业务代码中使用魔法数字或字符串做枚举比较** — 使用枚举类的 `getCode()` 方法，switch/if 中不出现裸数字

### 禁止写法

```java
// ❌ 字符串枚举
@Schema(description = "合同类型", example = "ELECTRONIC")
private String contractType;

// ❌ 字符串比较
if ("PAPER".equals(contractType)) { ... }
```

### 正确写法

```java
// ✅ 数字枚举
@Schema(description = "合同类型: 1=纸质合同, 2=电子合同, 3=不签合同", example = "2")
private Integer contractType;

// ✅ 枚举常量比较
if (OrderEnums.OrderContractTypeEnum.PAPER_CONTRACT.getCode().equals(contractType)) { ... }
```

## 补充规则

- **禁止因参考 VO/DTO 使用 String 枚举名或魔法字符串比较而照搬** — 须按本章 Integer `code` 规范独立设计，参考实现与本章冲突时以本章为准
