---
description: 操作红线 — 禁止执行的危险操作；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 操作红线

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 允许范围

- 只修改**项目源代码目录内**的文件
- 只操作当前工作区内的资源

### 禁止行为

#### 文件操作

- **禁止修改构建产物** — `target/`、`build/`、`dist/` 等目录下的文件不可手动修改
- **禁止修改其他项目** — 不得越界操作工作区外的文件
- **禁止修改 IDE 全局配置** — 不修改用户级 IDE 设置

#### 破坏性命令

以下操作**必须经用户明确确认**才能执行：

- `DROP TABLE` / `DROP DATABASE`
- `TRUNCATE TABLE`
- `DELETE` 不带 WHERE 条件
- `rm -rf` 或批量删除文件
- 修改数据库生产连接配置
- 强制推送 `git push --force`

#### 数据安全

- 禁止在代码中硬编码密码、密钥等敏感信息
- 禁止将敏感信息写入日志或提交到版本控制

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
