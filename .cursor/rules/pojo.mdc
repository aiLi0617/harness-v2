---
description: POJO 字段设计规范
globs: "**/*.java"
alwaysApply: false
---

# POJO 规范

## POJO 类规则

- **布尔属性禁止加 `is` 前缀** — `private Boolean deleted` 而非 `private Boolean isDeleted`，框架反序列化会解析为属性名 `deleted` 导致获取不到
- **属性必须使用包装数据类型** — `Long`/`Integer`/`Boolean` 而非 `long`/`int`/`boolean`，数据库查询结果可能 null，自动拆箱 NPE
- **RPC 方法参数和返回值必须使用包装类型**
- **局部变量推荐使用基本数据类型**
- **禁止设定任何属性默认值** — 如 `createTime = new Date()` 可能在更新其他字段时被意外覆盖
- **必须写 toString 方法** — 继承时前面加 `super.toString()`
- **禁止同时存在 `isXxx()` 和 `getXxx()`** — 框架无法确定优先调用哪个
- **serialVersionUID 新增属性时不要修改** — 避免反序列化失败；完全不兼容升级才修改

## 补充规则

- **禁止照搬参考 POJO/VO 的字段形态** — 如 `is` 前缀布尔属性、基本类型字段、类内默认值等；参考实现与本章冲突时以本章为准
