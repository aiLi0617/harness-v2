---
description: 编译检查门禁；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 编译检查门禁

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 规则

- **任何代码变更后必须确认编译通过**
- 编译失败必须**立即修复**，再继续后续工作
- 禁止在编译未通过的状态下进行下一步变更

### 执行方式

变更代码后执行编译命令确认：

- Maven: `mvn compile -q`
- Gradle: `gradle compileJava`

### 编译失败处理

1. 阅读编译错误信息，定位失败原因
2. 修复编译错误
3. 重新编译确认通过
4. 确认通过后再继续其他工作

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
