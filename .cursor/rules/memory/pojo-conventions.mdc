---
description: POJO 与 OOP 规范
globs: "**/*.java"
alwaysApply: false
---

# POJO 与 OOP 规范

## POJO 类规则

- **布尔属性禁止加 `is` 前缀** — `private Boolean deleted` 而非 `private Boolean isDeleted`，框架反序列化会解析为属性名 `deleted` 导致获取不到
- **属性必须使用包装数据类型** — `Long`/`Integer`/`Boolean` 而非 `long`/`int`/`boolean`，数据库查询结果可能 null，自动拆箱 NPE
- **RPC 方法参数和返回值必须使用包装类型**
- **局部变量推荐使用基本数据类型**
- **禁止设定任何属性默认值** — 如 `createTime = new Date()` 可能在更新其他字段时被意外覆盖
- **必须写 toString 方法** — 继承时前面加 `super.toString()`
- **禁止同时存在 `isXxx()` 和 `getXxx()`** — 框架无法确定优先调用哪个
- **serialVersionUID 新增属性时不要修改** — 避免反序列化失败；完全不兼容升级才修改

## BigDecimal 规范

- **禁止 `new BigDecimal(double)`** — 存在精度损失，`new BigDecimal(0.1F)` 实际值为 0.1000000014901...

```java
// ✅ 正确
BigDecimal a = new BigDecimal("0.1");
BigDecimal b = BigDecimal.valueOf(0.1);

// ❌ 错误
BigDecimal c = new BigDecimal(0.1);
```

- **等值比较用 `compareTo()`** — `equals()` 会比较精度，`1.0` 与 `1.00` 返回 false
- **货币金额以最小单位整型存储** — 如用 `Long` 存分而非 `BigDecimal` 存元

## 浮点数比较

- **基本类型不能用 `==`，包装类型不能用 `equals`**
- 方案一：指定误差范围 `Math.abs(a - b) < 1e-6F`
- 方案二：使用 BigDecimal 运算后 compareTo

## Integer 比较

- **全部使用 `equals()`** — -128~127 范围内 `==` 可用（IntegerCache），超出范围 `==` 返回 false
- DO 类属性类型须与数据库字段类型一一匹配 — bigint 对应 Long，不能用 Integer

## equals 调用

- **用常量或确定有值的对象调用** — `"test".equals(param)` 防止 param 为 null 时 NPE
- 推荐 `java.util.Objects#equals(Object a, Object b)`

## 访问控制

- 工具类构造方法必须 private
- 类非 static 成员变量：本类用 private，子类共享用 protected
- static 成员变量仅本类用必须 private，考虑是否 final
- 类成员方法只供内部调用必须 private

## 覆写规则

- **所有覆写方法加 `@Override`** — 防止 `getObject()` 和 `get0bject()` 混淆
- 过时接口加 `@Deprecated` 并说明替代方案
- 禁止使用过时类或方法

## 其他 OOP 规则

- 静态变量/方法直接用类名访问，不通过对象引用
- 可变参数放在参数列表最后，类型避免 Object
- 外部调用的接口不允许修改方法签名
- 构造方法禁止业务逻辑，初始化放 init 方法
- `final` 用于不允许继承的类、不允许修改的引用、不允许覆写的方法、不允许重新赋值的局部变量

## ORM 映射

- **不用 resultClass 当返回参数** — 每个表必须有对应 `<resultMap>`
- **禁止用 HashMap/Hashtable 接收查询结果** — 不同数据库版本解析类型可能不一致

## 分层领域模型

| 对象 | 说明 |
|------|------|
| DO (Data Object) | 与数据库表一一对应，通过 DAO 层向上传输 |
| DTO (Data Transfer Object) | Service/Manager 向外传输 |
| BO (Business Object) | Service 层封装业务逻辑 |
| VO (View Object) | Web 向模板渲染引擎层传输 |
| Query | 查询对象，超过 2 个参数封装，禁止 Map 传输 |

## 补充规则

- ⚠️ 禁止强制类型转换（强转）：禁止在业务代码中使用 `(Type)` 对对象做强转；必须通过泛型约束、明确返回类型或适配转换方法消除强转需求
