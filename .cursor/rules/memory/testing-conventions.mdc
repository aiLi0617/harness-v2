---
description: 测试规范
globs: "**/*Test*.java,**/*Spec*.java"
alwaysApply: false
---

# 测试规范

## 命名规范

- **测试类命名** — 被测类名 + `Test`（如 `OrderServiceTest`），对齐阿里巴巴手册
- **测试方法命名** — 项目内可用 `should_xxx_when_yyy` 或 `testXxx` 风格，须语义清晰

```java
@Test
void should_return_user_when_id_exists() { ... }

@Test
void should_throw_exception_when_email_is_invalid() { ... }
```

## 结构规范

每个测试方法遵循 **Given-When-Then** 结构：

```java
@Test
void should_calculate_total_when_items_provided() {
    // Given — 准备测试数据
    var items = List.of(new Item("A", 100), new Item("B", 200));

    // When — 执行被测方法
    var total = calculator.calculateTotal(items);

    // Then — 验证结果
    assertThat(total).isEqualTo(300);
}
```

## 基本原则

- **单元测试不依赖外部服务** — 数据库、HTTP、消息队列等使用 Mock
- **关键业务路径必须覆盖** — 正常流程 + 主要异常分支
- **测试独立性** — 测试之间无顺序依赖，可单独运行
- **测试数据自包含** — 不依赖共享测试数据，每个测试自行准备

## 禁止行为

- 禁止测试中使用 `Thread.sleep` 等待异步结果
- 禁止测试中硬编码环境相关配置（如 IP、端口）
- 禁止空断言的测试（仅验证不抛异常不算有效测试）

## AI 易忽略的单元测试规则

- **单元测试遵守 AIR 原则** — Automatic（自动化）、Independent（独立性）、Repeatable（可重复）
- **禁止 System.out 人肉验证** — 必须使用 assert 断言
- **遵守 BCDE 原则** — Border（边界值）、Correct（正确输入）、Design（结合设计文档）、Error（强制错误输入）
- **数据库相关测试** — 不假设数据已存在，用程序插入准备数据；设定自动回滚或使用特定前缀标识测试数据
- **核心业务增量代码须有单元测试** — 新增代码影响原有单元测试须及时修正
- **测试代码只能在 `src/test/java`** — 不允许写在业务代码目录
- **核心模块语句覆盖率和分支覆盖率达 100%** — 整体语句覆盖率至少 70%
