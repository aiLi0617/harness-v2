---
description: 控制语句与分支规范
globs: "**/*.java"
alwaysApply: false
---

# 控制语句规约

## switch

- 每个 `case` 须以 `break`/`continue`/`return` 终止，或注释说明 fall-through 目标
- **必须包含 `default`**，且放在最后（即使为空）
- `switch` 变量为外部传入的 **String** 时，**必须先判 null**（使用 `Objects.isNull` 等工具方法）

## if / else / 循环

- **必须使用大括号** — 即使只有一行，禁止 `if (x) doSomething();`
- if-else 嵌套不超过 **3 层** — 超过时用卫语句、策略模式或状态模式
- 表达异常分支时，优先 early return，减少 `else`
- **避免取反逻辑** — 用 `if (x < 628)` 而非 `if (!(x >= 628))`
- **禁止在条件表达式中插入赋值**

## 条件可读性

- 复杂条件先赋给有意义的布尔变量，再放入 `if`
- 除常见 `getXxx`/`isXxx` 外，避免在 `if` 内执行复杂语句或深层调用

## 循环性能

- 循环体内避免：定义对象/变量、获取 DB 连接、不必要的 try-catch（能外移则外移）
- 循环内字符串拼接用 `StringBuilder.append`，禁止在循环内使用 `+` 拼接

## 并发场景

- 高并发下避免用「等于」作为中断条件 — 用 `>=`/`<=` 区间判断

## 接口入参保护

批量操作、开放接口、敏感入口须做参数校验（类型、范围、长度、格式）。

## 补充规则

- if 条件须简化：多条件组合提取为语义明确的布尔变量或方法
