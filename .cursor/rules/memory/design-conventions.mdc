---
description: 系统设计规约（对齐阿里巴巴 Java 开发手册黄山版第七章）
globs: "**/*.java,docs/artifacts/*.md"
alwaysApply: false
---

# 设计规约

约束需求/设计阶段的建模、评审与架构原则（用例图、状态图、时序图、弱依赖与降级等）。

## 【强制】评审与文档

- **存储方案与底层数据结构** — 须评审一致通过并沉淀文档；缺陷易导致扩展性下降与迁移成本陡增
- 评审内容须含：存储介质选型、表结构是否满足方案、读写性能与空间、字段/索引关系、命名与类型
- 数据结构变更（如新增字段）也须评审通过后上线，必要时 double check

## 【强制】建模与图示（需求/设计阶段）

| 触发条件 | 必须使用的图 |
|----------|--------------|
| 交互 User 超过一类且 UserCase 超过 5 个 | 用例图 |
| 业务对象状态超过 3 个 | 状态图（明确状态转换触发条件） |
| 功能调用链涉及对象超过 3 个 | 时序图（明确各环节输入输出） |
| 模型类超过 5 个且依赖复杂 | 类图（明确类间关系） |
| 超过 2 个对象协作且流程复杂 | 活动图 |

## 【强制】弱依赖与降级

- 设计时须识别**弱依赖**（第三方降级/屏蔽后不影响主干流程，仅影响展示/通知等非关键能力）
- 须针对性设计**降级与应急预案**，保证核心系统可用
- 下游 RT 或错误率超阈值时，应自动降级/熔断，禁止仅向前端抛「服务器开小差」而无预案

## 【推荐】架构设计目标

- 确定系统边界（技术层面做与不做）
- 确定模块间依赖与宏观输入输出
- 确定指导后续演化的原则
- 确定非功能性需求（安全、可用、可扩展等）

## 【推荐】需求与设计

- 评估**异常流程与业务边界**，不只考虑主干
- **单一职责** — 类设计与实现符合单一原则
- **组合优于继承** — 不得已继承须符合里氏代换
- **依赖倒置** — 尽量依赖抽象类与接口
- **开闭原则** — 对扩展开放，对修改闭合
- **DRY** — 共性逻辑抽取公共模块/配置/类/方法，避免重复代码

## 【参考】文档与演进

- 设计文档作用：明确需求、理顺逻辑、后期维护；次要才是指导编码
- 可扩展性本质：**找到变化点并隔离变化点**
- 设计本质：**识别并表达系统难点**（规则、抽象、知识体系、表达力）
- **代码不能替代完整文档** — 深度调用链、模块依赖、业务场景、非功能需求须文档呈现
- 敏捷 ≠ 无设计 — 关键设计与文档沉淀仍需要

## 【参考】无障碍产品设计

- 可交互控件须可被 Tab 聚焦，焦点顺序符合自然操作逻辑
- 验证码除图形外须提供其它验证方式
- 自定义控件须明确交互方式（可用 `role` 等）
