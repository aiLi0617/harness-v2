---
description: 依赖管理规范
globs: "**/pom.xml,**/build.gradle*"
alwaysApply: false
---

# 依赖管理

## 基本原则

- **新增依赖需说明理由** — 注释或提交信息中解释为什么引入
- **锁定版本号** — 禁止使用 `LATEST`、`RELEASE` 或范围版本
- **优先使用已有依赖** — 引入新依赖前确认现有依赖是否已覆盖需求
- **定期检查漏洞** — 使用 OWASP Dependency-Check 或类似工具

## 版本管理

- 父 POM 或 BOM 统一管理版本，子模块不重复声明版本号
- Spring Boot 相关依赖跟随 `spring-boot-dependencies` BOM
- 三方库版本变更需评估兼容性

## 依赖范围

- `compile` — 运行时必需
- `provided` — 容器提供（如 Servlet API）
- `test` — 仅测试使用
- `runtime` — 仅运行时需要（如 JDBC 驱动）

## 禁止行为

- 禁止引入功能重叠的多个库（如同时引入 Gson 和 Jackson 做 JSON 处理）
- 禁止引入未维护的废弃库
- 禁止在业务代码中直接依赖测试框架

## 二方库规约（阿里巴巴手册）

- **GAV 规则** — GroupId：`com.{公司/BU}.业务线.[子业务线]`（最多 4 级）；ArtifactId：`产品线名-模块名`；Version：`主.次.修订`，**起始须为 1.0.0**
- **禁止线上依赖 SNAPSHOT**（安全包除外）；RELEASE 须中央仓库查证、不可覆盖升级
- **二方库升级**须 `dependency:resolve` 前后比对，仲裁变化须评估并用 `<exclude>` 排除冲突 jar
- **二方库接口返回值禁止枚举或含枚举的 POJO**
- **定制包版本** — 在版本号后加 `-英文说明序号`（如 `1.0.0-SCM1`）
- **依赖二方库群时**须统一版本变量（如 `${spring.version}`）
- **禁止同一 GroupId+ArtifactId 不同 Version** 出现在子模块 pom
- **【推荐】** 依赖声明放 `<dependencies>`，版本仲裁放 `<dependencyManagement>`
- **【推荐】** 二方库尽量不增加配置项；慎用不稳定 Utils
