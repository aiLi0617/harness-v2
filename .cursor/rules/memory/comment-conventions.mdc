---
description: 注释规范
globs: "**/*.java"
alwaysApply: false
---

# 注释规范

## 核心原则

- **只注释 why，不注释 what** — 代码本身应说明做了什么，注释解释为什么这样做
- **代码即文档** — 优先通过良好命名和结构减少注释需求

## 必须有注释

- **公共 API 必须 Javadoc** — 所有 public 类和方法必须有 Javadoc 注释
- **复杂算法** — 非直觉的业务逻辑或算法需注释说明意图
- **临时方案** — 标注 `// TODO:` 或 `// FIXME:` 并附带原因和计划

## 禁止行为

- **禁止注释掉的代码** — 用版本控制管理历史代码，不要注释保留
- **禁止冗余注释** — 如 `// 获取用户` 后面跟 `getUser()`
- **禁止过时注释** — 修改代码时必须同步更新注释
- **禁止日志式注释** — 如 `// 2024-01-01 张三修改`，这是版本控制的职责

## 注释模板

### 类注释 / 接口注释 / 枚举注释

```java
/**
 * ---------------------------
 * @Description: 简要描述
 * ---------------------------
 * @author: {git config user.name}
 * @date: yyyy年MM月dd日 HH:mm
 * @version: v1.0
 * ---------------------------
 */
```

### 方法注释

```java
/**
 * ---------------------------
 * @Description: 简要描述
 * ---------------------------
 * @params paramName 参数说明
 * @return 返回值说明
 * ---------------------------
 * @author: {git config user.name}
 * @date: yyyy年MM月dd日 HH:mm
 * @version: v1.0
 * ---------------------------
 */
```

> **@author 取值**：使用 `git config user.name` 的值。

## AI 易忽略的注释规则

- **所有类必须有 @author 和 @date** — 格式 `@date yyyy年MM月dd日 HH:mm`
- **所有抽象方法（含接口方法）必须 Javadoc** — 说明做什么、实现要求、调用注意事项
- **所有枚举字段必须有注释** — 说明每个数据项的用途
- **方法内单行注释在被注释语句上方另起一行** — 使用 `//`；多行注释用 `/* */`，与代码对齐
- **注释双斜线与内容间有且仅有一个空格** — `// 正确` 而非 `//错误`
- **TODO/FIXME 须标注人与时间** — `// TODO(张三, 2026-04-16): 需要实现 xxx`
- **删除无用字段、方法、内部类** — 方法中删除未使用的参数和变量
- **注释掉的代码** — 若非后续恢复（须在上方用 `///` 说明原因），则直接删除

## 补充规则

- ⚠️ 所有方法（含 private）必须严格按照上方"方法注释"模板写 Javadoc——包括 `@Description`、`@params`、`@return`、`@author`、`@date`、`@version` 以及分隔线，缺任何一项视为不合格。生成代码时必须逐方法检查注释完整性，不允许出现无注释的方法
- 代码块中关键业务逻辑必须有行注释，说明该段逻辑的业务含义；不是简单复述代码，而是解释"为什么这么做"或"这段在处理什么业务场景"
- 接口（interface）方法的 Javadoc 必须严格遵循方法注释模板（含 `@Description`、`@params`、`@return`、`@author`、`@date`、`@version` 及分隔线），不能只写一行描述或留空
- `@Override` 重写方法**不添加方法注释**——Javadoc 只写在接口/父类的方法声明上，实现类中 `@Override` 方法直接保留 `@Override` 注解即可，不重复写注释
- 关键主流程方法（如下单、结算、同步）必须添加“阶段化行注释”，至少覆盖校验、数据准备、持久化、外部调用、收尾五类阶段，便于维护者快速定位流程
- 行注释覆盖范围不得遗漏子方法/辅助方法：只要方法体内包含以下任一动作，均需对应位置写行注释，不得"只给主方法加、子方法裸奔"：
    - 调用外部 API / RPC / Feign（注释说明调用目的与依赖方）
    - Stream 流处理（注释说明过滤、映射、聚合的业务含义）
    - 批量数据装配、分组（注释说明分组维度与用途）
    - 校验分支（注释说明校验规则与失败后果）
    - 事务边界、异步提交（注释说明一致性策略）
- 多步有序流程（通常 ≥ 3 步）的方法体内必须使用"编号式行注释"建立阅读锚点：
    - 格式：`// 1. <步骤业务含义>` / `// 2. <步骤业务含义>` ...
    - 编号与方法出口/阶段日志的 `stage[X]` 对齐，便于日志-代码互相定位
    - 每个编号下方紧跟对应代码块，不允许编号后出现无关代码或空步
- 行注释内容规则：写"为什么 / 在处理哪个业务场景 / 规则来源"，不写"在做什么"——例如写 `// 严格租户隔离，不跨租户补查` 而非 `// 按 id 查合约`
