---
description: Java 代码格式规范
globs: "**/*.java"
alwaysApply: false
---

# 代码格式规范

## 缩进与编码

- **4 个空格缩进**，禁止使用 Tab
- 文件编码 **UTF-8**
- 换行符使用 **Unix 格式**（LF），不用 Windows CRLF

## 大括号与空格

- 空块写 `{}`；非空块：左 `{` 前不换行、后换行；右 `}` 前换行；`else` 等紧跟 `}` 不换行
- `if/for/while/switch/do` 与 `(` 之间加空格
- 二目、三目运算符左右加空格（含 `=`、`&&`、`+` 等）
- 左小括号与字符间、右小括号与字符间不加空格
- 类型强制转换：`)` 与转换值之间不加空格
- 多参数逗号后必须加空格：`method(a, b, c)`

## 行宽与换行

- 单行不超过 **120** 字符；超出时：
  - 第二行相对第一行缩进 4 空格
  - 运算符、方法调用 `.` 与下文一起换行
  - 多参数在逗号后换行
  - **禁止在括号前换行**

## 注释格式

- `//` 与注释内容之间**有且仅有一个空格**
- 方法内单行注释在被注释语句**上方**另起一行

## 其它

- 没有必要通过对齐等号来对齐变量赋值
- 不同逻辑/语义/业务代码块之间插入**一个**空行，禁止多个连续空行
- long/Long 赋值使用大写 **`L`**，禁止小写 `l`
- 浮点常量后缀使用大写 **`D`** 或 **`F`**

## 补充规则

- 单个方法总行数不超过 **80** 行（不含注释中的方法签名、大括号、空行；对齐阿里巴巴手册【推荐】）
