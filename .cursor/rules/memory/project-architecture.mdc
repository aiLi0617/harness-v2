---
description: 项目架构与分层结构
globs: "**/*.java"
alwaysApply: false
---

# 项目架构与分层结构

## 分层结构

项目采用 Orch + API + BIZ 三层 Service 架构：

- **Controller 层** — 接收请求、参数校验、调用 API Service
- **Orch Service 层（编排层）** — 跨服务编排，通过 Feign 调用 BIZ 服务；尽量将业务逻辑下沉到 BIZ 层，以减少 Feign 调用次数和 Orch 服务自身压力
- **API Service 层（中转层）** — 纯中转，不允许包含服务编排或业务逻辑
- **BIZ Service 层（业务层）** — 核心业务逻辑、事务管理、数据校验；尽量减少 Service 之间的相互调用
- **Mapper 层** — 数据持久化
- **Domain/Entity 层** — 领域模型
- **DTO/VO 层** — 数据传输对象
- **common-framework** — 跨模块共享的常量、工具类、基础枚举

## 模块职责

> 待适配：根据实际项目填写各模块名称与职责边界

| 模块 | 职责 | 备注 |
|------|------|------|
| 待补充 | 待补充 | 待补充 |

## 依赖方向

- 上层可依赖下层，禁止反向依赖
- Controller → API Service（中转）→ Orch Service（编排）→ BIZ Service → Mapper → Entity
- 跨模块调用通过 Service 接口，禁止直接访问其他模块的 Mapper
- 有其他方案存在，绝对不可以使用递归

## 补充规则

- Orch 编排层尽量避免出现业务判断、数据计算、状态流转等逻辑，应下沉到 BIZ 层
- 三层职责澄清：①Orch Service 是编排层，尽量将逻辑下沉到 BIZ，目的是减少 Feign 调用次数和 Orch 服务压力（并非所有逻辑都必须下沉，简单逻辑且不增加 Feign 调用的可保留）；②API Service 是纯中转层，严禁包含编排或业务逻辑；③BIZ Service 内部尽量减少 Service 之间的相互调用