---
description: 错误码规约（ErrorCode 常量与编号段）
globs: "**/*ErrorCode*.java,**/*Constants*.java,**/*.java,docs/artifacts/api-contract.md"
alwaysApply: false
---

# 错误码规约

## 常量定义格式

错误码统一定义在模块的 `*ErrorCodeConstants`（或项目约定的错误码常量类）中，使用 `ErrorCode` 类型：

```java
ErrorCode SERVICE_BAD_REQUEST = new ErrorCode(1_002_003_001, "数据不存在");
```

### 命名

- 常量名：**UPPER_SNAKE_CASE**，语义清晰，见名知义（如 `SERVICE_BAD_REQUEST`、`CART_ITEM_NOT_EXISTS`）
- 禁止魔法数字 — 业务代码通过常量引用，禁止在 Service/Controller 中硬编码数字

### 构造与字段

- 标准构造：**`(int code, String message)`** — 数字编码 + 默认提示文案
- `message` 供开发排查与日志；**不得**直接作为面向用户的唯一提示（用户文案走 VO/统一响应中的提示字段）
- 抛出方式：`ServiceExceptionUtil.exception(ErrorCodeConstants.XXX)` 或项目统一的 `ServiceException` 封装

## 数字编码格式

- 类型：**int**，源码书写使用**下划线分段**提升可读性（编译期等价于纯数字）
- 业务异常错误码共 **10 位**，分成 **四段**

```text
1_002_003_001
│  │   │   └── 第四段：错误码（3 位）
│  │   └────── 第三段：模块（3 位）
│  └────────── 第二段：系统类型（3 位）
└───────────── 第一段：类型（1 位）
```

### 第一段：类型（1 位）

| 值 | 含义 |
|----|------|
| `1` | 业务级别异常 |
| `x` | 预留 |

### 第二段：系统类型（3 位）

| 编码 | 系统 |
|------|------|
| `001` | 用户系统 |
| `002` | 商品系统 |
| `003` | 订单系统 |
| `004` | 支付系统 |
| `005` | 优惠券系统 |
| `...` | 按规划表扩展 |

### 第三段：模块（3 位）

- **不限制规则** — 各系统内按模块自行分段
- 一般建议：同一系统下多个模块再分子段

用户系统示例：

| 编码 | 模块 |
|------|------|
| `001` | OAuth2 模块 |
| `002` | User 模块 |
| `003` | MobileCode 模块 |

### 第四段：错误码（3 位）

- **不限制规则**
- 一般建议：每个模块内 **001 起自增**

### 区间声明（防重复）

各模块错误码区间须在**统一错误码规划类**中声明，用于解决跨模块编号冲突、避免重复：

- **只声明区间，不做实际使用** — 规划类仅登记「某系统-某模块」占用的号段
- 各模块实际错误码定义在各自的 `*ErrorCodeConstants` 中
- 新增模块须先在规划类登记号段，再在模块常量类中定义具体 ErrorCode

示例：

| 常量 | 编码 | 解读 |
|------|------|------|
| `SERVICE_BAD_REQUEST` | `1_002_003_001` | 业务异常 · 商品系统(002) · 模块 003 · 001 |
| `USER_NOT_EXISTS` | `1_001_002_001` | 业务异常 · 用户系统(001) · User 模块(002) · 001 |

## 制定原则

- **快速溯源** — 从编码可判断类型、系统、模块
- **禁止随意新增** — 新码须先在统一规划类登记号段，再在模块 `*ErrorCodeConstants` 定义，并在 API 契约或错误码附表中登记
- **编号不与组织架构挂钩** — 按模块规划表先到先得，变更须评审
- **语义稳定** — 已发布错误码不得改含义；废弃码保留，新场景用新序号

## 开放 API 与前后端约定

- 统一响应体中的业务错误码字段（如 `code` / `errorCode`）使用 **ErrorCode 的 int 值**（JSON 为数字），与常量定义一致
- 须同时提供：**HTTP 状态码** + **数字 errorCode** + **errorMessage**（开发排查）+ **用户提示**（终端可见，不得仅暴露 errorMessage）
- JSON 字段名 **lowerCamelCase**（如 `errorCode`，禁止 `ERROR_CODE`）

## 禁止行为

- 禁止将错误码文案直接作为用户唯一提示
- 禁止在业务代码中 `throw new RuntimeException("数据不存在")` 替代 ErrorCode
- 禁止跨模块复用他人模块序号段

## 补充规则

- 新增错误码前先在模块附表检索是否已有同语义编码
- 第三方错误须转义为本系统 ErrorCode，原始第三方码写入 errorMessage 或日志上下文
