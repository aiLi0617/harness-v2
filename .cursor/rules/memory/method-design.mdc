---
description: 方法设计规范
globs: "**/*.java"
alwaysApply: false
---

# 方法设计

## 规模约束

- **方法体不超过 80 行**（不含空行和注释；对齐阿里巴巴手册【推荐】）
- **参数不超过 3 个** — 超过时必须封装为参数对象（见下方参数对象模式）
- 过长方法必须提取子方法，每个子方法有明确的职责命名

## 单一职责

- 一个方法只做一件事
- 方法名应能完整描述其行为，无需阅读实现
- 如果方法名需要用 `and` 连接，说明职责过多

## 设计要点

- **尽早返回** — 使用 guard clause 减少嵌套层级
- **避免副作用** — 查询方法不应修改状态
- **避免布尔参数** — 布尔参数通常意味着方法做了两件事，拆分为两个方法
- **返回值语义清晰** — 避免返回魔术值（如 -1 表示未找到），使用 Optional 或抛异常

## 参数对象模式

参数超过 3 个必须封装为参数对象，不接受例外：

```java
// ❌ 参数过多
void createOrder(Long userId, String productId, int quantity, String address, String couponCode)

// ✅ 封装参数对象
void createOrder(CreateOrderRequest request)
```

## 事务管理

- **事务边界在 Service 层** — Controller 和 Mapper 不管理事务
- **事务范围尽量小** — 只包裹必须原子化的操作
- **只读操作标注 `@Transactional(readOnly = true)`**
- **`@Transactional` 不滥用** — 影响 QPS，需考虑缓存回滚、搜索引擎回滚、消息补偿等

## AI 易忽略的方法设计规则

- **正则表达式须预编译** — `Pattern` 对象定义为类的 `static final` 常量，禁止在方法体内 `Pattern.compile()`
- **禁止 Apache BeanUtils** — 性能差，使用 `Spring BeanUtils` 或 `Cglib BeanCopier`（均为浅拷贝）
- **循环体内用 StringBuilder 拼接字符串** — 禁止在循环中用 `+` 拼接 String，每次循环会 new 一个 StringBuilder
- **getter/setter 中禁止业务逻辑** — 增加排查问题难度
- **构造方法禁止业务逻辑** — 初始化逻辑放 init 方法
- **类内方法定义顺序** — public/protected > private > getter/setter
- **同名方法按顺序放置** — 多个构造方法或重载方法相邻排列
- **split 结果须检查尾部** — `"a,b,c,,".split(",")` 结果长度为 3 而非 5，须检查最后一个分隔符后有无内容
- **慎用 Object.clone** — 默认是浅拷贝，深拷贝需覆写 clone
- **Math.random() 返回 double** — 范围 `0 ≤ x < 1`，取整数随机数用 `Random.nextInt()`
- **枚举属性字段必须 `private final`** — 不可变
- **数据结构构造或初始化须指定初始容量** — 避免无限增长吃光内存
- **及时清理不再使用的代码段或配置** — 暂时注释的代码须用 `///` 说明理由

## 补充规则

- ⚠️ 方法参数超过 3 个必须封装为请求对象（Request/Param/DTO），无任何例外——生成代码时必须逐方法检查参数个数，超过 3 个立即封装，不允许以"先实现后优化"为由跳过
- if 判断条件必须简化：多个条件组合时提取为语义明确的布尔变量或方法，禁止在 if 中堆砌长表达式
- 禁止在同一方法中对同一数据源执行重复查询——如果前面已查询过某个值（如 `countByCartId`），后续需要最新值时应在本地累加/计算，而非重新调用远程接口查询
- 方法签名格式必须与项目现有风格一致——一行能放下的参数列表不换行；生成代码前先查看同文件或同 Controller 已有方法的签名格式，保持对齐
- 涉及跨服务调用的写操作方法必须添加事务注解——本项目使用 Seata 分布式事务，跨服务写操作统一使用 `@GlobalTransactional(rollbackFor = Throwable.class)`；添加前先查看同 Service 中已有方法的事务写法，保持一致
- ⚠️ 大事务优化必须采用“两阶段”结构：先在事务外完成查询/校验/组装，再进入事务执行最小化写路径（落库+状态更新+必要同步），禁止把大量只读远程调用放进事务路径
- 涉及第三方调用（如云商）时，必须抽取独立私有方法封装“请求对象构建 + 调用 + 返回转换”，业务主流程只保留编排代码，避免超长代码块
- 能并行的校验或查询必须并行化（如本地SKU、合约信息、云商SKU查询），并提供统一的并行结果收敛与异常出口
- 主流程方法（如 submit/checkout）禁止堆积仅供子方法使用的查询结果；若数据只在单个子方法内使用，应在该子方法就近查询并就近消费，降低主方法参数与维护成本
- ⚠️ 任务主流程改动禁止遗漏关键校验：必须同时覆盖业务规则校验（如状态/范围/可见性）与归属性校验（如用户/会员/租户/主体归属）；未满足“双校验”不得提交
- ⚠️ 引用既有类 / 接口 / DTO / 返回值类型时，**编码前必须用 Read 工具实际读取该类源码**确认方法签名、字段名与类型，禁止凭类名/方法名猜测。典型反例：`loginUser.getId()`（实为 `getUserId()`）、`AgreementSkuRespDTO.taxIncludedPrice`（实为 `price`）、`stockQty: Integer`（实为 `remainQuantity: BigDecimal`）；一次任务内多处引用同一外部类的，至少首次必须核对，之后的引用以首次核对结果为准
- 方法实现禁止遗留 `throw new UnsupportedOperationException()` 占位就进入联调；若依赖上游尚未提供的能力，必须先完整扩展上游链路（Mapper → Service → Api），或在 `docs/artifacts/impl-plan.md` 明确标记为阻塞项
- 调用链换行遵循“可读性优先”：普通调用（如 `api.getByXxx(...).getCheckedData()`）在不超出可读范围时允许保持单行；仅在链式操作较长或包含多阶段流处理（尤其 `stream` 的 `filter/map/collect/forEach`）时，按语义分段换行并保持缩进对齐，禁止为机械对齐而过度拆行
