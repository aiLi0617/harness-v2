---
description: API 设计规范
globs: "**/*Controller*.java,**/*Api*.java"
alwaysApply: false
---

# API 设计规范

## RESTful 风格

| 操作 | HTTP 方法 | URL 示例 |
|------|-----------|---------|
| 查询列表 | GET | /api/v1/users |
| 查询详情 | GET | /api/v1/users/{id} |
| 创建 | POST | /api/v1/users |
| 更新 | PUT | /api/v1/users/{id} |
| 部分更新 | PATCH | /api/v1/users/{id} |
| 删除 | DELETE | /api/v1/users/{id} |

## 统一响应体

> ⚠️ 待适配具体项目

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

## HTTP 状态码语义

- `200` 成功、`201` 创建成功、`204` 无内容
- `400` 参数错误、`401` 未认证、`403` 无权限、`404` 资源不存在
- `500` 服务器内部错误

## 版本管理

> ⚠️ 待适配：URL 路径方式 `/api/v1/` 或 Header 方式

## 其他要求

- 分页查询必须使用框架统一分页对象，详见下方分页规范
- 接口幂等性：POST 以外的操作应天然幂等，POST 操作需考虑幂等设计
- 敏感接口需要认证和权限校验

## AI 易忽略的前后端交互规则

- **Long 类型可直接返回前端** — 项目已做全局序列化处理，Long 类型会自动转为 String，无需手动转换
- **JSON 所有 key 必须 lowerCamelCase** — 小写字母开头，如 `errorCode` 而非 `ERROR_CODE` / `error_code`
- **列表接口为空时返回空数组 `[]` / 空集合 `{}`** — 不返回 null，减少前端 null 判断
- **错误响应须包含** — HTTP 状态码 + 数字 errorCode（对应 `ErrorCodeConstants` 中的 int 编码）+ errorMessage（开发排查）+ 用户提示信息
- **errorCode 为整数** — 与 `ErrorCode` 常量 `code` 字段一致，JSON 序列化为数字；禁止 A/B/C 五位字符串方案
- **错误码不直接给用户看** — errorMessage 给开发排查，用户提示信息给终端用户
- **URL 参数不超过 2048 字节** — 浏览器最小限制
- **URL 路径规范** — 名词复数、小写、下划线分隔、禁止携带 `.json`/`.xml` 后缀
- **翻页入参 < 1 返回第一页** — 后端入参大于总页数返回最后一页
- **时间格式统一** — 前后端统一 `yyyy-MM-dd HH:mm:ss`，统一 GMT
- **服务端返回用 JSON 格式** — 不用 XML
- **公开接口须入参保护** — 批量操作须限制数量，防止内存溢出
- **【强制】body 传参须控制长度** — 超出 nginx/tomcat 默认限制会导致解析失败
- **【强制】服务器内部重定向 forward** — 外部重定向 URL 统一代理生成
- **【推荐】响应须标记是否可缓存** — 如 `Cache-Control: s-maxage=...`
- **【参考】版本号放在 HTTP 头** — 不在 URL path 中加版本，利于向前兼容

## 分页规范

项目已封装统一分页对象，**禁止在 VO/DTO 中自行定义 `pageNo`、`pageSize`、`total`、`list` 等分页字段**。

### 请求端

- 使用 `com.broker.framework.common.pojo.PageParam`（含 `pageNo`、`pageSize`，自带校验注解）
- 如需额外查询条件，**继承** `PageParam` 添加业务字段
- Controller 中可使用 `@ModelAttribute PageParam pageParam` 或 `@RequestParam` 手动接收

```java
// 推荐：继承 PageParam
@Data
@EqualsAndHashCode(callSuper = true)
public class XxxPageReqVO extends PageParam {
    @Schema(description = "名称")
    private String name;
}
```

### 响应端

- 使用 `com.broker.framework.common.pojo.PageResult<T>`（含 `list: List<T>` + `total: Long`）
- Controller 返回类型固定为 `CommonResult<PageResult<XxxVO>>`
- Service 返回类型固定为 `PageResult<XxxVO>`
- 空结果使用 `PageResult.empty()`

```java
// Controller 签名
CommonResult<PageResult<XxxVO>> getXxxPage(XxxPageReqVO reqVO);

// Service 签名
PageResult<XxxVO> getXxxPage(XxxPageReqVO reqVO);

// 空结果
return PageResult.empty();
// 有数据
return new PageResult<>(list, total);
```

### 禁止写法

```java
// ❌ 禁止：VO 中自行定义分页字段
public class XxxListRespVO {
    private List<XxxVO> list;
    private Integer pageNo;
    private Integer pageSize;
    private Long total;
}

// ❌ 禁止：Controller 返回自定义分页 VO
CommonResult<XxxListRespVO> getXxxList(...);
```

## 枚举值规范

项目中所有枚举**一律使用 Integer 数字编码**（枚举类的 `code` 字段），**禁止在 VO/DTO/API 中使用字符串枚举名**。

### 规则

1. **前后端传输使用数字** — 请求和响应中的枚举字段类型为 `Integer`，值为枚举类的 `code`
2. **VO/DTO 字段类型为 Integer** — 不使用 `String` 存储枚举值
3. **@Schema 注释中标注含义** — `@Schema(description = "合同类型: 1=纸质合同, 2=电子合同, 3=不签合同")`
4. **API 契约文档中标注映射关系** — 枚举定义表必须列出数字编码、含义、对应后端枚举类
5. **禁止在业务代码中使用魔法数字或字符串做枚举比较** — 使用枚举类的 `getCode()` 方法，switch/if 中不出现裸数字

### 禁止写法

```java
// ❌ 字符串枚举
@Schema(description = "合同类型", example = "ELECTRONIC")
private String contractType;

// ❌ 字符串比较
if ("PAPER".equals(contractType)) { ... }
```

### 正确写法

```java
// ✅ 数字枚举
@Schema(description = "合同类型: 1=纸质合同, 2=电子合同, 3=不签合同", example = "2")
private Integer contractType;

// ✅ 枚举常量比较
if (OrderEnums.OrderContractTypeEnum.PAPER_CONTRACT.getCode().equals(contractType)) { ... }
```

## 补充规则

- 接口必须严格使用 RESTful 规范——GET 查询、POST 创建、PUT 更新、DELETE 删除，不允许用 POST 代替所有操作
- API 层及 biz 层 Service 方法不返回基本数据类型（Long、Integer、Boolean 等），统一返回业务对象或 VO/DTO；仅在明确需要获取单独数量（count）或 ID 时才允许返回基本类型
- 分页接口必须使用框架 `PageResult<T>` + `PageParam`，禁止在 VO 中自行定义 `pageNo/pageSize/total/list` 等分页字段
- 枚举值必须使用 Integer 数字编码（枚举类 `code` 字段），禁止在 VO/DTO/API 中使用字符串枚举名（如 "ELECTRONIC"、"TRADING" 等）
- `@PathVariable` 禁止冗余声明：当方法参数名与 URL 路径变量名一致时，**不得**在注解括号内重复写名称：
    - ❌ 冗余：`@PathVariable("skuCode") String skuCode`
    - ✅ 正确：`@PathVariable String skuCode`
    - 仅在路径变量名与参数名确实不一致时（如路径是 `{user-id}` 而参数是 `userId`）才保留 `@PathVariable("user-id")`
    - 路径变量由后端自身定义在 `@RequestMapping` 中，可控性高，同名情况下冗余属于反模式
- `@RequestParam` 允许（甚至推荐）显式命名：前端查询参数名常与 Java 驼峰变量名习惯不一致（如 `page_no` vs `pageNo`），显式写 `@RequestParam("pageNo") Integer pageNo` 有利于：
    - 解耦前端入参命名和 Java 变量命名习惯
    - 避免依赖 `-parameters` 编译参数，行为在不同构建环境下一致
    - 可读性更强，code review 时一眼看清请求参数名
    - 因此 `@RequestParam("xxx") Xxx xxx` 不视为冗余，不强制清理
- **RPC/`*Api` 层禁止返回 `Map` 类型**（包括 `Map<String, String>`、`Map<Long, XxxDTO>` 等任意泛型形态）：
    - 原因：
        - Map 是弱类型容器，字段语义完全丢失，调用方必须阅读实现才能推断 key/value 含义
        - JSON 序列化对 Map key 强制转 String，非 String 类型 key（Long、Integer、枚举）在跨服务反序列化时不可靠
        - Swagger / OpenAPI 文档无法展示 key/value 的语义字段
        - 未来新增第 3 个字段时必须破坏性地改签名
    - ✅ 正确：定义强类型 `XxxRespDTO`，RPC 返回 `List<XxxRespDTO>`，消费侧自行 `Collectors.toMap`
    - ❌ 禁止：`CommonResult<Map<String, String>> getXxxMap(...)`
    - 同样适用于 biz 层对外暴露的 `*Api` 和 `*Service` 接口；仅 **内部私有工具方法** 可使用 Map 作为中间数据结构
