---
description: OBS 对象存储目录规划规范
globs: "**/*Obs*.java,**/*Storage*.java,**/*Upload*.java,**/*File*.java"
alwaysApply: false
---

# OBS 目录规范

## 基本原则

- **每个环境一套目录** — dev / sit / prod 等隔离，禁止混用路径
- **项目启动前完成目录规划** — 研发过程中持续维护更新

## 规划表字段

| 字段 | 说明 |
|---|---|
| 环境 | dev / sit / prod 等 |
| 用途 | 服务器日志、制品、业务附件等 |
| 桶 | OBS 桶名，如 `yunshang-cloud-dev` |
| 路径 | 对象前缀，如 `/server/cce/`、`/build/backend/{service}/{流水号}/` |
| 描述 | 目录用途说明 |
| 模块路径 | 可选，与代码模块对应 |
| 模块 | 可选，所属微服务/模块 |

## 路径设计

- 路径须见名知义，体现**环境 + 业务 + 用途**
- 制品目录建议含服务名与流水号/版本号，便于追溯
- 禁止在代码中硬编码生产桶名或路径，须走配置中心/环境变量

## 安全要求

- 敏感文件禁止公共读
- 用户私有文件须与租户/用户 ID 隔离
- 上传须校验文件类型、大小；下载须校验访问权限

## 补充规则

- 新增 OBS 上传/下载能力前，先在规划表中登记桶、路径、用途，再写代码
- 路径常量统一定义在模块常量类或配置中，禁止在 Service 内散落字符串
