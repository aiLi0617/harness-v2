---
description: 变更实施规则
globs: 
alwaysApply: true
---

# 变更实施规则

## 基本流程

1. **先写测试再改代码** — 新增功能先编写失败测试，再实现使测试通过
2. **渐进式变更** — 大改动拆成多个小步骤，每步可独立验证
3. **每步可验证** — 每次提交后确保编译通过、测试通过

## 禁止行为

- **禁止一个提交包含多种不相关改动** — 功能代码和重构不混在同一提交
- **禁止大规模未测试变更** — 一次性改动超过 200 行须拆分
- **禁止跳过验证** — 不能"先全部改完再一起测"

## 变更验证清单

每次变更后按顺序检查：

1. ✅ 编译通过
2. ✅ 现有测试全部通过
3. ✅ 新增测试覆盖变更点
4. ✅ Lint 无新增告警
5. ✅ diff 中无无关改动

## 补充规则

- **禁止「就近抄代码」**（规则优先于参考实现）：
  - 编码前须先按 `rules-loader.mdc` 加载**当前场景**规则，再对照 LLD/DDL 设计；**禁止**以同包、同模块、同目录下的已有文件作为规范来源
  - 允许参考的仅限：分层位置、包路径、类名后缀、方法命名风格；**禁止**照搬参考文件的实现细节（含 MyBatis `<sql>`/`<include>` 结构、异常处理方式、判空写法、日志格式、事务边界）
  - 发现参考实现与已加载 rules **冲突**时，以 rules 为准重写，不得因「项目里已有类似写法」而沿用违规模式
- **禁止「参考实现整段抄代码」**：Javadoc 元数据（`@author`/`@date`）、错误码号段、常量/魔法值、`ignore-urls`/`ignore-tables` 等配置须对照**当前任务已加载 rules** 重新填写，不得原样继承参考类
- **批量新建多文件时**：禁止用单一参考类的注释块或类头模板批量套用到全部新文件；每个新类须独立执行元数据生成（见 comments 规则）
