---
description: 阶段间输入输出契约
globs: 
alwaysApply: true
---

# 阶段间输入输出契约

> 所有子代理在执行前**必须**先读取本文件，确认自己所处阶段的输入和输出要求。

## 产出物根目录

所有阶段产出物统一存放在 `docs/artifacts/` 下，任务完成后归档到 `docs/artifacts/archive/{日期}-{任务简称}/`。

## 契约总览

```
[可选: 云文档→prd-source.md] → PRD拆分 → feature-list.md → HLD → hld.md → DDL → ddl.md ──┐
                                                                              └→ API → api-contract.md ─┤
                                                                                                         ├→ LLD → lld.md → Plan → impl-plan.md
```

## 各阶段契约

### 0. PRD 导入（可选）

| 项目 | 内容 |
|---|---|
| **输入** | 云文档 URL（飞书文档、Confluence 等）或用户指定的外部文件 |
| **输出** | `docs/artifacts/prd-source.md` |
| **跳过条件** | 用户直接口头描述需求时可跳过 |

### 1. PRD 功能拆分（prd-splitter）

| 项目 | 内容 |
|---|---|
| **输入** | `docs/artifacts/prd-source.md`（如存在）或用户口头需求描述 |
| **输出** | `docs/artifacts/feature-list.md` |
| **必含章节** | 功能清单、优先级矩阵、验收标准、上下文摘要 |

### 2. 概要设计（architect-hld）

| 项目 | 内容 |
|---|---|
| **输入** | `feature-list.md`（必读） |
| **输出** | `docs/artifacts/hld.md` |
| **必含章节** | 系统架构、模块划分、模块职责与边界、接口概览、非功能性需求、决策记录、上下文摘要 |
| **一致性约束** | 功能清单中的每个功能点必须映射到至少一个模块 |

### 3. 数据库设计（db-ddl）

| 项目 | 内容 |
|---|---|
| **输入** | `feature-list.md` + `hld.md`（必读） |
| **输出** | `docs/artifacts/ddl.md` |
| **必含章节** | ER 图描述、表结构定义、索引策略、数据迁移说明、决策记录、上下文摘要 |
| **一致性约束** | 表/字段命名与 HLD 模块中使用的领域术语一致 |

### 4. API 契约设计（api-contract）

| 项目 | 内容 |
|---|---|
| **输入** | `feature-list.md` + `hld.md`（必读） |
| **输出** | `docs/artifacts/api-contract.md` |
| **必含章节** | 接口清单、请求/响应结构、错误码定义、决策记录、上下文摘要 |
| **一致性约束** | 接口必须与 HLD 接口概览一一对应；DTO 字段与 DDL 字段语义一致 |

### 5. 详细设计（lld-author）

| 项目 | 内容 |
|---|---|
| **输入** | `feature-list.md` + `hld.md` + `ddl.md` + `api-contract.md`（全部必读） |
| **输出** | `docs/artifacts/lld.md` |
| **必含章节** | 类设计、方法签名、业务流程、异常处理策略、决策记录、上下文摘要 |
| **一致性约束** | 类名/方法名与 HLD 模块对应；SQL 操作与 DDL 表结构匹配；Controller 方法与 API 契约匹配 |

### 6. 实现计划（impl-planner）

| 项目 | 内容 |
|---|---|
| **输入** | `lld.md`（必读），`hld.md`（参考） |
| **输出** | `docs/artifacts/impl-plan.md` |
| **必含章节** | 子任务列表、依赖关系、执行顺序、验证方式、上下文摘要 |
| **一致性约束** | 每个子任务必须追溯到 LLD 中的具体类或方法 |

## 输入文件缺失时的处理

如果必读输入文件不存在：
1. **停止执行**，不要基于猜测继续
2. 向调用方报告缺失的文件路径
3. 等待上游阶段补齐后再启动
