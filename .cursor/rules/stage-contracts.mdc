---
description: 跨阶段制品通用约束；具体阶段 I/O、生产者、消费者和顺序以 Workflow YAML 为唯一来源；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 阶段制品通用约束

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 唯一来源

- Workflow YAML 是阶段 `entry_artifacts`、`exit_artifacts`、生产者、消费者、顺序、并行和回退关系的唯一来源。
- Agent 只声明输入语义、输出内容和完成标准，不复制逐阶段路径表。
- 本 Rule 不维护 Agent 到制品的映射表，不引用具体 Agent 名称。

### 任务隔离

- 每次 Workflow 启动必须确定 `task_id` 与 `artifact_root=docs/artifacts/work/{task-id}`。
- 阶段内所有制品路径相对 `artifact_root` 解析。
- 禁止向 `docs/artifacts/` 根目录直接写活动制品。
- 禁止读取其他任务目录的同名制品作为当前输入。

### 阶段入口

- 进入阶段前逐项验证 `entry_artifacts.required` 存在且结构完整。
- `entry_artifacts.optional` 不存在时允许继续，但必须记录降级影响。
- 必需输入缺失、损坏或与 `workflow-state.md` 冲突时停止并触发人工检查点。
- 不得用对话记忆替代缺失的权威制品。

### 阶段出口

- 阶段完成前验证全部 `exit_artifacts` 已生成且满足对应 Agent 完成标准。
- 同一固定制品在当前 Workflow 中只能有一个权威生产步骤。
- 下游不得直接修补上游制品；问题必须回退给 Workflow 指定的生产步骤。
- Reviewer 只追加自己的质量报告，不修改被审查制品。

### 质量记录

- 同一 gate 的复审在原报告追加 `Check NNN`，禁止覆盖旧 Check。
- 机械证据在原报告追加 `Run NNN`，禁止覆盖旧 Run。
- `quality-gate-reviewer` 只能在所有必跑和已触发专项报告完成后裁决。
- 当前有效状态取文件中最后一个完整 Check 或 Run。

### 归档

- 只有最终质量门禁和验证满足 Workflow 条件时才能归档完成。
- 归档以整个 `docs/artifacts/work/{task-id}` 目录为单位移动。
- `docs/artifacts/archive/**` 是历史证据，禁止为适配新结构而重写。

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
