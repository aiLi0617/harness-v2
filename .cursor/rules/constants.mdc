---
description: Java 常量组织规范；在处理匹配文件或相关任务时使用
globs: "**/*.java"
alwaysApply: false
---

# 常量组织规范

## 适用范围

- 当引用、读取或修改匹配 `**/*.java` 的文件，或任务语义明确涉及本规则领域时适用。

## 强制规则

### 禁止魔法值

- 代码中不允许出现未定义含义的字面量（数字、字符串），必须抽取为常量

### 分类与维护

- **按功能分类维护常量** — 禁止单一大常量类堆砌所有常量（如 `CacheConsts`、`ConfigConsts` 分开）
- **常量统一维护在 `common-framework` 模块中** — 禁止在业务模块中散落定义常量

### 复用层次

- 跨应用使用 — 二方库 client
- 应用内 — 子模块 constant
- 子工程内 / 包内 / 类内 — `private static final`

### 常量与枚举的边界

- **数据库字段对应的固定值** — 使用枚举（命名为 `{数据表名}Enum`）
- **非数据库字段的固定值** — 使用常量类，不使用枚举

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
