---
description: Java 常量组织规范
globs: "**/*.java"
alwaysApply: false
---

# 常量组织规范

## 禁止魔法值

- 代码中不允许出现未定义含义的字面量（数字、字符串），必须抽取为常量

## 分类与维护

- **按功能分类维护常量** — 禁止单一大常量类堆砌所有常量（如 `CacheConsts`、`ConfigConsts` 分开）
- **常量统一维护在 `common-framework` 模块中** — 禁止在业务模块中散落定义常量

## 复用层次

- 跨应用使用 — 二方库 client
- 应用内 — 子模块 constant
- 子工程内 / 包内 / 类内 — `private static final`

## 常量与枚举的边界

- **数据库字段对应的固定值** — 使用枚举（命名为 `{数据表名}Enum`）
- **非数据库字段的固定值** — 使用常量类，不使用枚举
