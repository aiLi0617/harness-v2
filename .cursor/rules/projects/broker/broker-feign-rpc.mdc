---
description: broker 项目 Feign RPC 契约（@FeignClient / ApiConstants / 结果解包）约定
globs:
  - "**/broker-modules-api/**/*Api.java"
  - "**/broker-modules-biz/**/*ApiImpl.java"
  - "**/broker-orch/**/*RpcConfiguration.java"
alwaysApply: false
---

# broker Feign RPC 契约

## `@FeignClient` 声明（强制带 url）

每个 `broker-module-*-api` 的 Feign 接口统一三要素：`name = ApiConstants.NAME` + `contextId = "接口类名"` + `url = "${feign.client.url.<服务名>:}"`。

```java
@FeignClient(name = ApiConstants.NAME, contextId = "OrderApi", url = "${feign.client.url.settlement-biz-server:}")
@Tag(name = "RPC 服务 - order - 订单表")
@Validated
public interface OrderApi {
    String PREFIX = ApiConstants.PREFIX + "/order";
}
```

- ⚠️ **`url` 不可省略**：必须写 `url = "${feign.client.url.<对应 biz 服务名>:}"`（占位符默认空，便于按环境直连/走注册中心）。新增或修改 `*Api` 时务必带上，历史遗漏需补齐
- `<服务名>` 取该模块 `ApiConstants.NAME`（= 对端 `spring.application.name`，如 `settlement-biz-server`、`product-biz-server`）
- `contextId` 取接口类名，保证同一 `name` 下多个 Feign 客户端 bean 不冲突
- 路由前缀统一 `String PREFIX = ApiConstants.PREFIX + "/{业务}"`；`ApiConstants.PREFIX = RpcConstants.RPC_API_PREFIX + "/capa-layer/{模块}"`

## ApiConstants（每个 api 模块一份）

```java
public class ApiConstants {
    public static final String NAME = "settlement-biz-server"; // 与对端 spring.application.name 一致
    public static final String PREFIX = RpcConstants.RPC_API_PREFIX + "/capa-layer/settlement";
    public static final String VERSION = "1.0.0";
}
```

## 接口与实现约定

- `*Api` 方法返回值统一 `CommonResult<T>`；分页用 `CommonResult<PageResult<T>>`，入参/出参用 `*DTO`
- biz 侧 `*ApiImpl` 实现 `*Api`，标 `@RestController`，仅做「转换 + 调 Service + `success(...)` 包装」
- 新建一个 `*Api` 后，必须在调用方 orch 的 `RpcConfiguration` 的 `@EnableFeignClients(clients = {...})` 中登记该 `Api.class`，否则注入不到

## 调用方解包结果

Feign 调用返回 `CommonResult<T>`，取数据用 `getCheckedData()`（错误时抛 `ServiceException`），禁止不校验直接 `getData()`：

```java
OrderRespDTO order = orderApi.getById(id).getCheckedData();
```

## 补充规则

- RPC/`*Api` 接口禁止返回 `Map`，必须用强类型 `*DTO`；忽略租户的方法以 `IgnoreTenantId` 结尾并在对端 `application.yaml` 的 `ignore-urls` 同步登记
