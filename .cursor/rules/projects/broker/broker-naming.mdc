---
description: broker 项目命名特化（云商 Yunshang 前缀 / 一文件一类）约定；在处理匹配文件或相关任务时使用
globs: "**/com/broker/**/*.java,**/broker-*/**/*.java"
alwaysApply: false
---

# broker 项目命名特化

## 适用范围

- 当引用、读取或修改匹配 `**/com/broker/**/*.java,**/broker-*/**/*.java` 的文件，或任务语义明确涉及本规则领域时适用。

## 强制规则

- 云商接口对象统一带 `Yunshang` 前缀；DTO/VO 默认一文件一类，禁止用"类中类"承载可复用对象

### 云商对接命名

- ⚠️ 云商开放接口（yunshang-http 模块及其调用链）使用的请求/响应/视图对象，类名必须统一添加 `Yunshang` 前缀（如 `YunshangBatchSkuListingInfoReqDTO`、`YunshangSkuListingInfoVO`），禁止使用无前缀命名

### 一文件一类

- ⚠️ 尽量避免"类中类"定义 DTO/VO（尤其云商接口对象）；默认一文件一类，嵌套类仅在强绑定且不可复用场景才允许，并需在代码注释说明原因

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
