---
description: broker 项目 Redis Key 定义与 RedisDAO 访问约定；在处理匹配文件或相关任务时使用
globs: "**/com/broker/**/dal/redis/**/*.java,**/*RedisDAO.java,**/RedisKeyConstants.java"
alwaysApply: false
---

# broker Redis 访问约定

## 适用范围

- 当引用、读取或修改匹配 `**/com/broker/**/dal/redis/**/*.java,**/*RedisDAO.java,**/RedisKeyConstants.java` 的文件，或任务语义明确涉及本规则领域时适用。

## 强制规则

- 缓存 key 必须设置过期时间，禁止写入无 TTL 的长期 key（明确的常驻配置缓存除外，并在注释说明原因）

### Key 统一定义在 RedisKeyConstants

- 每个模块的 Redis Key 集中在 `{module}/dal/redis/RedisKeyConstants` **接口**中声明，**禁止**在 Service/DAO 里散落硬编码 key 字符串
- key 命名 `{模块前缀}:{语义名}`，分段用冒号；多段拼接用接口内的 `join(...)` 助手，**禁止**手写 `+ ":" +`
- 需要分布式锁/原子操作的 key 用 hash-tag 包裹固定段：`{operate_orch:lock}:%s`

```java
public interface RedisKeyConstants {
    static String join(CharSequence... elements) {
        return String.join(":", elements);
    }

    String CHECKOUT_TOKEN = "business_frontend:checkout_token";
    String AGREEMENT_SAVE_LOCK_KEY = "operate_orch:save_agreement";
}
```

### 通过 *RedisDAO 访问，不在 Service 直连 RedisTemplate

- 缓存读写封装在 `{module}/dal/redis/{domain}/Xxx RedisDAO`，标 `@Repository`，注入 `StringRedisTemplate`
- Service 层注入并调用 `*RedisDAO`，**禁止**在 Service 里直接操作 `RedisTemplate`/`StringRedisTemplate`
- 复杂对象用 `JsonUtils` 序列化为 String 存储；写入时显式带过期时间

```java
@Repository
public class CheckoutTokenRedisDAO {
    @Resource
    private StringRedisTemplate stringRedisTemplate;

    public void set(String token, CheckoutSnapshot snapshot, long expireMinutes) {
        String key = RedisKeyConstants.join(RedisKeyConstants.CHECKOUT_TOKEN, token);
        stringRedisTemplate.opsForValue()
            .set(key, JsonUtils.toJsonString(snapshot), expireMinutes, TimeUnit.MINUTES);
    }
}
```

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
