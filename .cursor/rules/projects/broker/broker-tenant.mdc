---
description: broker 项目租户隔离接入（IgnoreTenantId 命名 / ignore-urls 同步 / 历史命名识别口径）约定
globs:
  - "**/com/broker/**/*.java"
  - "**/broker-*/**/*.java"
  - "**/application*.yaml"
alwaysApply: false
---

# broker 租户隔离接入

> 本文件只约束 broker 工程内「忽略租户」接入的命名与配置同步。

## 忽略租户方法命名

- 调用三方服务和 MQ 时必须使用忽略租户的方式；忽略租户的方法以 `IgnoreTenantId` 作为方法名后缀（如 `sendMessageIgnoreTenantId`）；其余方法会自动携带租户 ID，不需要显式传参

## ignore-urls 配置同步

- ⚠️ 若存在"忽略租户 ID"的接口链接（URL），必须自动同步到对应服务的 `application.yaml` 中 `broker.tenant.ignore-urls` 配置，禁止遗漏或仅改代码不改配置

## 命名不标准的历史接口（识别口径）

- 混用多种写法：`ignore-tenant-id` / `no-tenant-id` / `ignore-tenantId` / `no-tenantId` / `IgnoreTenantId` / `NoTenantId` / `ignoreTenant`
- 存在 URL 末尾**完全无标识**但实际已配置在 `ignore-urls` 里的接口
- 反向存在：方法名/URL 带 `ignoreTenantId` 字样、但**未**在 `ignore-urls` 中声明 → 并非真正忽略租户，仍会被拦截器注入上下文租户（历史命名遗漏或滥用）
- **以配置为准，命名仅作线索**；发现"命名与配置不一致"时，在回复中明确标注"**接口命名不标准**"，提醒用户核对业务预期

## 补充规则

- 忽略租户的方法以 `IgnoreTenantId` 结尾，并必须在对端 `application.yaml` 的 `ignore-urls` 同步登记，二者保持一致
