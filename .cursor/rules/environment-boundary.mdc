---
description: 环境边界 — 开发环境隔离规则；在所有 Agent 任务执行时持续生效
globs:
alwaysApply: true
---

# 环境边界

## 适用范围

- 适用于所有 Agent 任务，作为持续生效的工程约束。

## 强制规则

### 基本原则

所有操作仅限于**开发环境**，严禁触及生产环境。

### 禁止行为

- **禁止连接生产数据库** — 只使用开发/测试环境数据库
- **禁止部署到生产环境** — 不执行任何指向生产环境的部署命令
- **禁止使用生产配置** — 不引用 `application-prod.yml` 等生产配置文件
- **禁止真实用户数据** — 测试数据不含真实用户个人信息

### 环境识别

操作前确认当前环境：

- 检查数据库连接地址是否为开发环境
- 检查配置文件的 `spring.profiles.active` 是否为 `dev` / `test`
- 检查部署目标地址是否为开发环境

### 测试数据要求

- 使用虚构数据（如 `test_user_001`）
- 手机号、邮箱等使用明显的测试值（如 `13800000001`、`test@example.com`）
- 禁止从生产环境拷贝数据到开发环境（可能含隐私数据）

## 验证清单

- 确认本规则适用范围内的强制约束均已落实。
- 确认没有出现本规则明确禁止的行为。
- 确认代码、配置或制品与本规则中的示例和领域约定一致。
