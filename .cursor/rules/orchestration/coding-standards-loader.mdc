---
description: 规则加载器
alwaysApply: true
---

# 规则加载器

> **架构原则**：规则文件互不引用，每条规则自描述本领域约束。「何时读哪些规则」由本文件、workflow YAML 与 globs 负责映射；纠正写入路由见 `correction-detection.mdc` 与 `memory-consolidator`。

在开始执行任务之前，必须先加载 `.cursor/rules/` 下与当前任务相关的规则。

## 加载流程

1. **扫描目录**：列出 `.cursor/rules/` 下所有子目录中的 `.mdc` 文件（不仅限于下方映射表中已列出的）
2. **匹配规则**：先按下方映射表加载已知规则，再检查扫描结果中是否有映射表未覆盖的新文件，根据文件名判断是否与当前任务相关
3. **读取内容**：用 Read 工具读取所有相关规则文件的完整内容
4. **严格遵守**：在后续执行过程中严格遵守所加载的规则

## 场景加载指引

> 与 `feedback/correction-detection.mdc`、`agents/shared/memory-consolidator.md` 的分类表保持同步,三处任一处新增分类时必须同步更新。

| 任务场景 | 必须加载的规则（按文件名） |
|---|---|
| 编写/修改 Java 代码 | `null-safety` `naming-conventions` `method-design` `collection-handling` `exception-handling` `comment-conventions` `code-style-format` `control-flow-conventions` `java-edit-self-check` |
| 涉及数据库/Entity/Mapper | `database-conventions` `schema-guard` `orm-mybatis-conventions` |
| 涉及 ER 图绘制/DDL 设计文档 | `er-diagram-spec` |
| 涉及 API/Controller | `api-design` `security-conventions` |
| 涉及多租户/跨租户数据访问/`ignore-urls`/`ignore-tables`/MQ 或三方回调接入 | `tenant-isolation` `message-queue-conventions` |
| 涉及并发/多线程 | `concurrency` |
| 涉及日期时间处理 | `datetime` |
| 涉及日志 | `logging` |
| 涉及 DTO/VO/Entity 设计 | `pojo-conventions` |
| 涉及测试代码 | `testing-conventions` |
| 涉及依赖/pom.xml | `dependency-management` |
| 涉及项目结构变更 | `project-architecture` `microservice-conventions` |
| 涉及 Redis 缓存 | `redis-caching-conventions` |
| 涉及 OBS/对象存储/文件上传下载 | `object-storage-obs-conventions` |
| 涉及应用安全/权限/脱敏/CSRF | `security-conventions` |
| 涉及 Git 分支操作 | `git-branch` |
| 涉及 Git 提交 | `git-commit` |
| 涉及变更实施流程/渐进式改动 | `change-implementation` |
| 涉及任务拆解/子任务规划 | `task-decomposition` |
| 涉及子代理产物交接 | `stage-contracts` |
| 涉及 MCP 配置/代理引用 MCP | `mcp-conventions` |
| 代码审查 | `naming-conventions` `method-design` `exception-handling` `logging` `comment-conventions` `change-scope-guard` |

多个场景重叠时,合并加载所有相关规则。

## 映射表维护

当在 `.cursor/rules/` 下**新增或删除** `.mdc` 规则文件时，必须同步更新上方映射表，保持映射表与实际文件一致。
