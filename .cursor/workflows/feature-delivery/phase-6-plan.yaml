# feature-delivery 工作流 · 阶段 6：实现计划
# 由 feature-delivery-workflow SKILL 按 feature-delivery.yaml 的 phases 顺序编排执行。
# Cursor IDE 不自动解析此 YAML。rules / agents / global_skills 继承 feature-delivery.yaml。
name: feature-delivery/phase-6-plan
description: 把详细设计转化为可执行的子任务清单
phase: 6
entry_artifacts:
  - docs/artifacts/lld.md
  - docs/artifacts/hld.md  # 参考
exit_artifacts:
  - docs/artifacts/impl-plan.md
prev_phase: feature-delivery/phase-5-lld
next_phase: feature-delivery/phase-7-code

steps:
  - name: plan
    description: 实现计划
    agent: agents/impl-planner
    skill: skills/writing-plans
    actions:
      - 读取 lld.md
      - 拆分子任务、标注依赖、判定可并行项
      - 输出到 docs/artifacts/impl-plan.md
